# Rules Registry —— ai-factory

> 规则晋升阶梯:E1(自觉)→ E2(检查脚本)→ E3(hook 强制)。同一 E1 违反 2 次必须晋升。

| # | 规则 | 等级 | 载体 | 日期 | 晋升历史 |
|---|---|---|---|---|---|
| R1 | 模板中构造带查询参数的链接必须用 url_for/urlencode 兜底,禁止裸拼 `href="/x?k={{ v }}"` | E2 | 检查:grep 模板 `href="[^"]*\?{{`(应为 0 命中) | 2026-08-29 | 源于 Dashboard v2 评审 A1(项目名含 & 静默截断) |
| R2 | 装配层消费 yaml 字段必须按"可为 None"设防(`or ""`),Ticket.load 不做校验 | E1 | 自觉 | 2026-08-29 | 源于评审 A2 |
| R3 | 需要文件元信息(mtime 等)时用 glob 拿到的真实路径,禁止用文件内容里的 id 重建路径 | E1 | 自觉 | 2026-08-29 | 源于评审 A3 |
| R4 | 把 id 列表链接化前先校验 id 形态(伪 id 如 system 不链接) | E1 | 自觉 | 2026-08-29 | 源于评审 A4 |
| R5 | 分组/聚合展示必须与验收标准逐条对照(本次漏"纯已完结项目也要有一行") | E1 | 自觉 | 2026-08-29 | 源于评审 A5 |
| R6 | 页面断言必须锚定该页独有特征,导航栏全站都有的字符串不能当断言 | E1 | 自觉 | 2026-08-29 | 源于评审:空转断言 |
| R7 | ai-init 后必须验证根 stack-profile.yaml 已实例化,否则 lint/test 闸空转 | E1 | 自觉 | 2026-08-29 | 本周期实测空转,已补建 |
| R8 | 任何分支合 main 前,必须用 main 线代码加载真实 pool 全部工单(数据-代码 schema 偏斜会让全站 500,cherry-pick 干净基线也躲不掉) | E2 | scripts/check-pool-load.py | 2026-08-29 | 源于 2026-08-29 p1_round 事故:integration 线写的字段 main 线 Ticket 没有,load 即 TypeError |
| R9 | 配额/现金闸可以放开,但必须"明放":闸 warn-only 需在配置显式标注磨合期+复审日期;台账必须照记(无精确值就估算),禁止记 0;闸可松、账不可瞎 | E1 | 自觉(orchestrator.yaml 标注 + 复审日期) | 2026-08-29 | 源于 boss 决策:dsh 磨合期允许临时放开;修订方向 T-2026-0829-003 |
| R10 | 凡产生代码/文档变更的任务必建编排工单,且**账本随执行全程同步**(双轨融合:工单=系统账本,技能包=过程闸,执行可 runner 可交互);交互式执行四步:p2_approved 即灌 tasks → 推进 p3 → 逐任务 done+verify → 按门禁走完 p4/p5;轻量例外(纯问答/typo 级)须显式说明免单 | E2 | scripts/check-pool-load.py:p3+ 状态 tasks 非空;p2_approved/p3_queued 挂 *_commit 即 FAIL;违背时按 mistake-retro 复盘 | 2026-08-29 | 源于 Dashboard v2 台账断流;**2026-09-01 E1→E2 晋升**:同日两起"建单后脱钩执行"(T-2026-0901-001 过门禁后 tasks 空跑完开发;T-2026-0901-002 hotfix done 但 tasks 空),触发"同 E1 违反 2 次必晋升" |
| R11 | 状态机推进必须读 agent **结论**(verdict),不得只认退出码:qa/审核角色产物必须显式含"通过/GO"判定才推进,否则挂起退回——退出码=done 只证明"程序跑完",不证明"事做对" | E1 | 自觉(立案 T-2026-0902-015 落 verdict 解析闸) | 2026-09-02 | 源于 T-2026-0902-010:QA p4 报告写"不通过/D1 P0",runner 仍推 p5_ready(runner.py:405 只判 role_run.status==done),带病工单递到 release 才炸 |
| R12 | resume 前必须比对本次与上次挂起 reason_code:同一确定性根因(平台 bug 不随重试消失)禁止裸 resume 连撞,须先修根因并显式确认 | E1 | 自觉(立案 T-2026-0902-016 落幂等提示) | 2026-09-02 | 源于 T-2026-0902-011:deploy 路径闸确定性误报被反复 resume 三连挂,incident T-2026-0902-014 复用三次 |
| R13 | 部署/配置里的路径必须与该资源的**真实布局**对齐(软链目标/挂载点/持久件),写闸前先在目标环境实测路径存在性;禁止凭命名直觉拼路径 | E1 | 自觉 | 2026-09-02 | 源于 T-2026-0902-011:deploy.py 依赖闸读 {app}/releases/current(服务器不存在,真实是 {app}/current 软链),恒读空恒误报 |
| R14 | 时序敏感测试(跨进程/helper/墙钟等待)等待窗须按"全套并发负载"裕度设定,不得按隔离单跑裕度;隔离过全套挂即窗口不足信号 | E1 | 自觉 | 2026-09-02 | 源于 T-2026-0902-010 D2:孙泄漏用例隔离 2.8s 过、全套 20s 窗超,放宽 45s 稳定 |

