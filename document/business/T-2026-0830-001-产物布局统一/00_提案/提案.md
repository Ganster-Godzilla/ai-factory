# 提案-T-2026-0830-001: 需求产物布局统一(文件夹化)

> Phase 0 动议(boss 2026-08-30 指示"搞")。本提案只写四项:问题/方向/范围/不做。
> 本单是新布局的第一个用户:本提案即存放于 `<工单号>-<需求短名>/00_提案/` 下。

## 问题

需求产物两套布局并存:T-2026-0829-001 的 ARTIFACT_MANIFEST 写死扁平
`document/business/<id>-prd.md`,技能流用 `<模块>/01_需求分析/` 文件夹。
扁平混排可扫读性差(sk-video-studio 的 business 目录已 7 文件混排),
且与"口径统一"初衷相悖(boss 2026-08-30 指出)。

## 方向

统一为**工单文件夹布局**:
`document/business/<工单号>-<需求短名>/00_提案/提案.md`
`document/business/<工单号>-<需求短名>/01_需求分析/{prd,功能清单,歧义澄清记录}.md`

- manifest 路径模板改为相对工单文件夹;闸门 glob `<id>-*/` 前缀解析文件夹(短名不参与机器判定,纯人读)。
- 存量迁移:ai-factory 与 sk-video-studio 的扁平文件与旧模块文件夹(dashboard/orchestrator/dsh-usage)全部 git mv 进工单文件夹,引用处(backlog/state/ticket.artifacts)同步改。
- 技能模板(phase1-requirements)与 CLAUDE.md 布局说明同步。

## 范围

1. artifacts.py 路径模板 + gates.py 文件夹解析(glob 前缀,多匹配报错)
2. 迁移脚本一次执行(两仓),git mv 保历史
3. plugin/skills/phase1-requirements 默认路径、CLAUDE.md、使用指南口径更新
4. manifest/gates 既有测试改新路径 + 新增文件夹解析用例

## 不做

- 不动 docs/specs/(设计/验收/发布类产物)布局——同模式候选,下次单议
- 不改工单数据模型、不改状态机、不改门禁逻辑(仅路径解析)
- 不动 sk 仓 T-2026-0829-005/007 工单状态与内容(只搬文件位置)
- T1-prd.md 等无名遗留文件原地保留(登记,不强迁)
