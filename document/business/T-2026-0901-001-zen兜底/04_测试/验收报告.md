# 验收报告:T-2026-0901-001 模型网关接入 OpenCode Zen(方案 B)

## 环境

- Windows 11,Node v24.14.0,Python 3.11;relay D:\Tool\kimi-relay.js(vbs 长驻,端口 8787)
- Zen 账户:余额 $30(2026-09-01 充值);key 在 D:\Tool\keys\opencode.env(gitignored)
- 设计变更:原 Anthropic 兜底方案实测证伪(/messages 全模型 500/disabled),经 boss 决策改方案 B(OpenAI 路由组),变更数据见 design.md 变更记录

## 范围

Z1 relay 三后端(后被方案 B 取代)/ Z2 水位隔离 / Z3 计价+对账脚本 / Z4 文档+全量验证 / Z5 端到端验证(含设计变更)/ Z6 R10 晋升 E2 / Z7 方案 B 实施

## 用例结果

| # | 用例 | 结果 | 证据 |
|---|------|------|------|
| 1 | relay failover 组保持双 kimi,zen 为独立路由组 | 过 | node --test 7/7 绿;/__status backends=[kimi1,kimi2]+routes.zen |
| 2 | 活集成:/zen/chat/completions kimi-k3 → 200 + OpenAI 形状 + stats 落账 | 过 | relay-zen.test.js 活测试(真实 key/余额);requests=1 input=86 output=16 |
| 3 | token 统计不被 gzip 绕过 | 过 | 复现 undici gzip → inputTokens=0;剥 accept-encoding 后 86/16 落账 |
| 4 | k3 水位只计 kimi*(zen 不计) | 过 | pytest test_gateway 7 绿(含 zen-only=0 用例);生产 /__stats zen weekIn=86 时 kimi2=4552581 互不影响 |
| 5 | 对账脚本:dry-run 估算 + 幂等 + estimated=true | 过 | test_zen_ledger 4 绿;生产 dry-run:zen 2026-W36 in=86 out=16 → ¥0.0036 |
| 6 | kimi 主链路回归 | 过 | claude -p "只回复两个字:就绪" → "就绪",is_error=false |
| 7 | GBK 控制台兼容(¥ 打印) | 过 | sys.stdout.reconfigure utf-8;dry-run 直接运行无 UnicodeEncodeError |
| 8 | R10 E2:check-pool-load 拦截脱钩工单 | 过 | 上线即抓 T-2026-0901-002(done tasks 空,已补录);当前 19 单 0 bad |

事故记录:钉选未验证后端致全停 8 分钟(2026-09-01),/__reset 恢复;教训入 ops-lessons#17,与本单 F1 变更直接相关。

## 结论

方案 B 全部验收用例通过,建议放行进入发布/观察窗。遗留(另立,不阻塞):dsh provider 接线 zen(F6)、claude k3 池 Zen 兜底翻译层评估(F7)、Zen 硬现金闸(F5)。
