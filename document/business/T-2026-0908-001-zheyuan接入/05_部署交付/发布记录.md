# T-2026-0908-001 发布记录:zheyuan 渠道三通道入网关

## 合并清单

| 位置 | 文件 | 变更 |
|---|---|---|
| D:\Tool 仓(commit `3ff536d`) | kimi-relay.js | BACKENDS+3(routeOnly);pickBackends 委托 relay-routes.pickChain;/__status 带 routeOnly |
| 同上 | relay-routes.js(新) | ROUTES 路由组纯函数模块(k2.6/gpt-6-astra 两链+缺省链) |
| 同上 | test/relay-routes.test.js(新) | 7 例(取链/冷却顺位/全冷/pinned/缺省回归锁) |
| 同上 | test/relay-keys.test.js / relay-zen.test.js | 名单 7→10;顺位用例改钉缺省链语义 |
| 同上 | test/smoke-zheyuan.js(新) | 六步活冒烟 |
| 同上(补遗 `e2822fc`/`8f73732`) | 0903-011 现场 | 冷却分级/settled 守卫/翻译层修补/冒烟(0903 已上线,补入账) |
| ai-factory 仓 | document/business/T-2026-0908-001-zheyuan接入/ | 提案/P1/P2/验收/发布全套 |

## 版本

D:\Tool @ `3ff536d`(生产 relay 已加载);ai-factory 仓仅文档,见本单文档目录。

## 部署清单

relay 未登记编排器 deploy 脚本,显式声明:部署 = D:\Tool 仓提交 + 生产 relay 重启加载新代码。
已执行:kill 8787 旧进程(PID 36008)→ `wscript D:\Tool\kimi-relay.vbs` 守护拉起 →
/__status 10 后端在线、缺省链七层、zy 三路 routeOnly、无冷却。
keys 零变更(zheyuan-k2/gpt/ds.env 先于本单入册);kimi 池四 key 与 glm/ds/zen key 全部原样保留。

## 冒烟结果

- 全量 node 测试:**30/30 绿**
- smoke-zheyuan 六步:**全过**(名单 10/三后端真实调用 200/k2.6 落点 zy-k2/k3 不进 zy)
- 生产路径 k2.6 真实调用:**200**(Anthropic 形状,stop_reason=end_turn)
- 水位口径:kimi* 周合计 109.3M 不变(渠道流量独立计量,不进 k3 闸)

## 回滚方案

```
cd /d/Tool && git revert 3ff536d && taskkill //PID <8787进程> //F && wscript kimi-relay.vbs
```

回滚后 zy 三后端消失、路由组消失,缺省链与 0902-009 行为完全一致;key 文件保留无影响
(未引用的 key 不加载)。ai-factory 仓文档保留无害。
