# 歧义澄清记录 — T-2026-0908-001

| # | 问题 | 结论 |
|---|---|---|
| Q1 | k2.6 链尾掉到 kimi 池时 model 会变成 k3,算语义漂移吗? | 不算。与既有溢出梯语义一致(glm/ds 层本就改写 model);熔断链尾=可用性优先于模型纯度,k3 兜底只会更强不会更弱 |
| Q2 | zy-k2 为什么不用 translate? | 渠道的 Pro Anthropic 组原生说 /v1/messages(探活实证),透传+model 重写即可,少一层翻译少一类 0903 事故面 |
| Q3 | zy-gpt 的 reasoning/thinking 块怎么处理? | 沿用 0903-011 翻译层(thinking 透传已修);本单不为 gpt 推理模型加新逻辑,攻坚质量归 T4 A/B 评 |
| Q4 | 免费渠道用量要不要进 k3 周水位闸? | 不进。k3_week_token_budget 是 kimi 订阅池水位;渠道用量经 /__stats 各自计量,W1 台账(T3)按周抄数 |
| Q5 | 路由组 key 用模型名(k2.6)会不会和上游真实模型名混淆? | 不会。k2.6 是 relay 客户端侧路由键(编排器 T2 会显式传);上游真实模型名 kimi-k2.6 在后端定义里重写,两不相干 |

无未决歧义。
