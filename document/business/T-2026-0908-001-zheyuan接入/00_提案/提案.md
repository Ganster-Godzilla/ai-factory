# 提案-T-2026-0908-001:zheyuan 渠道三通道入网关 + 按模型路由分组

> Phase 0 动议。分流减员计划(boss 2026-09-08 批准)的 T1 接入单。
> 依据:渠道探活实录(三 key 双协议全通,k2.6 速度同档 k3);分流方案(level→驾驶员映射)。

## 问题

k3 订阅 4 账号 ¥2796/月,合伙人免费渠道(折愿知心)三把 key 已入册 `D:\Tool\keys\zheyuan-*.env`
且探活全通,但**网关(D:\Tool\kimi-relay.js)不认识这三个后端**——流量仍全走 kimi 付费池,
分流无从谈起。同时 relay 只有单一 failover 顺位,无法按工单级别把 L3/L2 流量引去 k2.6。

## 方向

最小接入,官方 key 一把不删、顺位语义不变:

1. BACKENDS 增三通道:`zy-k2`(Anthropic 原生透传+model 重写 kimi-k2.6,复用既有
   `b.model` 透传改写路径)、`zy-gpt`/`zy-ds`(translate 翻译层,直接站在
   T-2026-0903-011 的全套修复上)。
2. 新增**按模型路由组**:请求 model='k2.6' → 渠道链(zy-k2→zy-ds→glm-flash→ds-flash
   →kimi1-4→zen-k3);model='gpt-6-astra' → zy-gpt 优先;其余 model → 现顺位零变化。
3. 熔断语义不动:FAILOVER_CODES/冷却分级/settled 守卫/pinned 全部沿用;
   官方 key(kimi 池/ds-flash/glm/zen)全部留在链尾当备份——渠道挂了自动掉回。

## 范围(粗略)

| # | 改动 | 文件 |
|---|---|---|
| 1 | BACKENDS +3(zy-k2/zy-gpt/zy-ds)+ keys 加载 | D:\Tool\kimi-relay.js |
| 2 | ROUTES 路由组表 + pickBackends(model) 按模型选链 | D:\Tool\kimi-relay.js |
| 3 | 名单断言 7→10、路由组断言、顺位断言(伪造冷却) | D:\Tool\test\relay-keys.test.js(+新增路由测试) |
| 4 | 活冒烟:三后端各 1 次真实调用 + k2.6 路由落点 + /__stats 计量 | D:\Tool\test\(新增 smoke) |

## 不做

- 不做编排器 level→model 传递(runner/adapter 改动)——T2 单
- 不做 dsh 适配器直切渠道、Claude Code 客户端指向切换——T2 单
- 不做金丝雀台账脚本(T3)、gpt-6 攻坚 A/B(T4)、退订执行(T5)
- 不动 kimi 池/glm/ds/zen 既有后端与 key;不动 zen 路由组
- 不做渠道侧计费/对账(免费账号无现金账;用量经 /__stats 周水位可见)

## 自估

改动 1 文件 + 测试,触运行路径(relay 是 k3 池咽喉)但回滚容易(revert+重启)。
按策略标准属 L1(风险=中,外部接口/运行路径)。定级 L1 走完整流程。
