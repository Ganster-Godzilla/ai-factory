# 提案:办公室阶段3(写操作:能看→能办)

- 工单:`pool/tickets/T-2026-0916-003.yaml`
- 类型:feature / 平台体验(沙盒只读 → 可办理)
- 续接:T-2026-0911-007(方块人沙盒已接通真实数据)

## 问题

沙盒已能"看"(六岗位/工单/KPI 真实数据),但**看到待办处理不了**——审批要跳老
dashboard,建单要敲 CLI。办公室要完成"看团队推进 → 处理审批和异常 → 验收交付"
的闭环,必须能在页面里直接办。

## 方向

沙盒内直接完成写操作,全部经编排器既有合法入口,不新开状态语义:

- **审批/驳回/恢复**:侧栏"待你处理"卡片直接批,走现有 `/approve`、`/reject`、
  `/resume` 路由(actor=boss 真实记录);写后 invalidate 缓存,下轮轮询即新。
- **网页建单**:选已登记项目、填目标/验收要求/限制,提交走 `orc new` 等价入口
  进 P0→P5 现有流程;保存草稿不触发执行。
- **交付物入口**:按项目/工单看产物(提案/PRD/设计/验收报告/发布记录),只读。

## 范围

沙盒 UI:待办卡审批按钮 + 建单表单 + 交付物浏览。后端复用现有路由,仅补
建单 API(new_ticket 包装)。写安全:全经 statemachine+save_ticket(pool .lock
协调),禁前端直改 YAML;过期页面操作返回"数据已变化,请刷新"提示。

## 不做

- 不做心跳/运行活性、全部暂停、进程终止、自动重试(阶段4,涉执行语义)。
- 不动 runner/watchdog;不改状态机迁移表;不绕 verdict 闸与 resume 幂等闸。
- 不做多人/远程访问(本机访问,认证部署另议)。
