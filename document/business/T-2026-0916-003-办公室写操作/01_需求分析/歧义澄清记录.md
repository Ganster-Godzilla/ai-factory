# 歧义澄清记录:办公室阶段3(写操作)

- 工单:`pool/tickets/T-2026-0916-003.yaml`

## 已澄清

| 问题 | 结论 | 依据 |
|---|---|---|
| 写操作走新语义吗? | 不走;审批/驳回/恢复与 HTML 路由共用 `_apply_approve` 等同一逻辑 | 单一事实源,防两处漂移 |
| 版本冲突怎么判? | 前端带卡片 last_update 作 base_ts,后端比当前最后事件 ts,不一致 409 | 与办公室卡片口径一致;一次读单事件文件,成本可忽略 |
| 建单会触发执行吗? | 不会;new_ticket 存 draft,后续走既有 P0→P5 | 提案"保存草稿不触发执行" |
| 产物路径安全? | resolve 后必须在项目登记目录内,否则 404 | 防路径穿越 |
| 缓存与写一致性? | 写后 invalidate_office_cache,下轮轮询即新 | 30s TTL 不会挡住动作结果 |

## 待澄清(后续阶段)

- 挂起工单恢复时的 reason 展示细化(同因强制按钮的提示文案)——阶段4统一处理。
- 心跳/全部暂停/终止/重试——阶段4,涉执行语义,不在本单。

## 无其余歧义

边界(复用既有状态语义、写安全经 statemachine+pool 锁、不做执行控制)无歧义。
