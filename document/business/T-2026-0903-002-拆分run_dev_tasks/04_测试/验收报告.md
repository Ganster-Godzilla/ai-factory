# 验收报告:run_dev_tasks 拆分(复杂度 46→可维护)

- 工单:`pool/tickets/T-2026-0903-002.yaml`
- 依据:`../01_需求分析/prd.md` 验收标准 + `../02_设计文档/design.md` Checkpoints
- 执行角色:qa(黑盒,只看验收命令与外部行为)
- 类型:纯重构(行为零变化)

## 环境

| 项 | 值 |
|---|---|
| 机器 | Windows 11 Home China (10.0.26200) |
| Python | 3.11.5 |
| 代码 | main @ `050946e`(S2/S3 提交) |
| 日期 | 2026-09-03 |

## 范围

- S1/S2/S3 三条切片验收命令逐条执行
- radon 复杂度前后对比
- 返回协议 12 串逐字核对

## 用例结果

| # | 用例 | 命令 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | S1 抽三闸 | `pytest tests/orchestrator/test_gate_enforcement.py tests/orchestrator/test_statemachine.py -q` | **通过** | `29 passed` |
| 2 | S2 抽派发+验收判负 | `pytest tests/test_runner_parent_alive.py tests/test_runner_watchdog.py -q` | **通过** | `8 passed` |
| 3 | S3 全套回归 | `pytest tests/ -q` | **通过** | `425 passed, 2 deselected, 359s, exit 0` |
| 4 | AC3 返回协议 12 串 | grep 源码核对 | **通过** | 12 串逐字在(见下) |
| 5 | AC5 ruff F 类 | `ruff check runner.py --select F` | **通过** | 0 新增(All checks passed) |

## radon 复杂度前后对比(核心证据)

| 函数 | 前(cc) | 后(cc) | 等级变化 |
|---|---|---|---|
| `run_dev_tasks` | **46 (F)** | **4 (A)** | F→A,达标(≤10) |
| `_lazy_load_tasks` | — | 5 (A) | ≤15 ✅ |
| `_completion_or_deadlock` | — | 5 (A) | ≤15 ✅ |
| `_cost_gate_blocked` | — | 8 (B) | ≤15 ✅ |
| `_dispatch_task` | — | 5 (A) | ≤15 ✅ |
| `_verify_task` | — | 13 (C) | ≤15 ✅ |
| `_fail_ladder` | — | 15 (C) | ≤15 ✅(卡线) |

## 返回协议 12 串(逐字保留,dev-loop.sh 依赖)

`suspended: 任务清单装载失败` / `auto: p4_verifying` / `blocked`(门禁)/ `suspend: 依赖死锁` / `blocked: ds 日现金线` / `suspend: 工单预算帽` / `task:{id}:done` / `retry:{id}:{attempts}` / `suspend: k3 配额超线` / `consult:{id}:{status}` / `suspend:{id}`(consult_exhausted)/ `suspend: 任务判负` / `task_failed:{id}` —— 全部在源码原位/抽出函数内逐字保留。

## 签名兼容

`run_dev_tasks(pool, ticket, adapter, project_dir, cfg=None, consult_adapter=None) -> str` 公开签名与返回类型不变;仅新增 `_` 私有函数;diff 仅 runner.py 该函数段,其他函数零改动。

## 结论

**通过。** 拆分达标:`run_dev_tasks` 圈复杂度 46→4(F 级→A 级),各辅助函数 ≤15;
全套 425 绿证明行为零变化;返回协议/事件/台账/签名全部兼容。dev 执行心脏从"上帝函数"
拆为职责单一、可测可审的小函数,后续改动不再在 46 条路径里赌博。
