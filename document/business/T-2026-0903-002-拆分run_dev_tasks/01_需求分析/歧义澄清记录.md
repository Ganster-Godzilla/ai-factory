# 歧义澄清记录:run_dev_tasks 拆分

- 工单:`pool/tickets/T-2026-0903-002.yaml`

## 已澄清

| 问题 | 结论 | 依据 |
|---|---|---|
| 是否改业务逻辑? | 否,纯结构调整;阈值/事件字段/返回字符串一律不动 | 提案"不做"首条;dev-loop.sh 依赖返回协议 |
| 行为零变化如何证? | 全套既有测试即行为契约,重构前后全绿;另逐字比对返回协议 | AC2/AC3 |
| 拆分粒度? | 五职责各一函数,`run_dev_tasks` 收薄为编排;验收/判负阶梯若仍超 15 可再拆 `_verify_task` | radon 阈值为判据 |
| 是否顺带拆其他上帝函数? | 否;main/check_gate/DshAdapter.run 另立单 | 控制爆炸半径,单工单单职责 |
| 签名是否兼容? | `run_dev_tasks` 公开签名与返回类型不变;只新增私有 `_` 函数 | AC6;现有测试直接调用它 |

## 待澄清(设计期定)

- `_dispatch_task` 内部是否再拆 `_verify_task`(scope/acceptance 验收段):
  视拆分后该函数 cc 是否 ≤ 15 定,超则拆,不超则留。

## 无其余歧义

边界(只动 runner.py 的 run_dev_tasks 及其抽出函数,不碰调用方)无歧义。
