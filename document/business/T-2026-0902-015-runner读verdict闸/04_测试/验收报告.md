# 验收报告:runner 推进读 QA verdict(不只认退出码)

- 工单:`pool/tickets/T-2026-0902-015.yaml`
- 依据:`../01_需求分析/prd.md` 验收标准 + `../02_设计文档/design.md` Checkpoints
- 执行角色:qa(黑盒,只看验收命令与外部行为)

## 环境

| 项 | 值 |
|---|---|
| 机器 | Windows 11 Home China (10.0.26200) |
| Python | 3.11.5 |
| 代码 | main @ `1121b23`(S3 提交) |
| 日期 | 2026-09-02 |

## 范围

- S1/S2/S3 三条切片验收命令逐条执行
- PRD 验收标准 AC1-AC6 逐条对照

## 用例结果

| # | 用例 | 命令 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | S1 parse_verdict 纯函数(词表) | `pytest tests/orchestrator/test_gates.py -q` | **通过** | `23 passed`(含 7 verdict 用例) |
| 2 | S2 P4 边 verdict 闸(fail-closed) | `pytest tests/orchestrator/test_gate_enforcement.py -q` | **通过** | `13 passed`(fail/unknown 拦、pass 放、缺章节报章节、非 P4 边不触发) |
| 3 | S3 verdict=pass 事件留痕 | `pytest tests/orchestrator/test_gates.py -q`(含 S3 用例) | **通过** | `test_p4_release_writes_verdict_pass_event` 绿 |
| 4 | AC1 报告写"不通过" → 不推进 | S2 用例 `test_p4_verdict_fail_blocks` | **通过** | GateFailed,fails 含 `verdict=fail` |
| 5 | AC2 报告写"通过" → 推进+留痕 | S3 用例 | **通过** | 推进 p5_ready,事件 verdict=pass |
| 6 | AC3 缺"结论"章节 → fail-closed | S2 用例 `test_p4_missing_conclusion...` | **通过** | FAIL 行为"缺少章节",非 verdict 行(顺序正确) |
| 7 | AC4 无法解析 → fail-closed | S2 用例 `test_p4_verdict_unknown_blocks` | **通过** | GateFailed `verdict=unknown` |
| 8 | AC5 现有 p4→p5 零回归 | `pytest tests/ -q` | **通过** | 全套绿(见下) |
| 9 | AC6 verdict 判定来源留痕 | S3 用例事件断言 | **通过** | verdict=pass 事件落事件流 |

## 全套回归

`pytest tests/ -q`(015+016 合并后终跑):**421 passed, 2 deselected, 360s, exit 0**;
`python scripts/check-pool-load.py`:**checked 54 tickets, 0 bad**。

## 结论

**通过。** 三条切片验收命令全绿,PRD AC1-AC6 逐条满足,fail-closed 语义(不通过/缺章节/无法解析一律挂起不放行)实证成立。R11 落地:状态机推进 p4→p5 现在读 QA 结论,不再只认退出码。
