# 提案:k3 共享池扩容(k3-c 第三账号)

## 问题

k3 双账号周额度频繁耗尽(xxx-coding 实测:FAILOVER 401 429 quota),今晚已触发 zen-k3 付费兜底。boss 新增第三个 k3 账号(k3-c),需入共享池 failover 组缓解额度压力。

## 方向

relay BACKENDS 追加 kimi3(k3-c),与 kimi1/kimi2 同语义:轮询候选、FAILOVER 冷却、week 水位统计入 150M 配额闸。

## 范围

- D:\Tool:kimi-relay.js BACKENDS 追加;测试断言更新(2→4 含 zen-k3 共 4 后端,其中 kimi* 3 个)
- ai-factory:guide 密钥清单同步;水位口径无需改(过滤是 kimi* 前缀)
- 生产:重启 relay + 钉选实测 + auto 恢复

## 不做

- 不改水位闸/费率/对账(口径不变);不动 zen-k3 兜底;不引入钉选策略变更
