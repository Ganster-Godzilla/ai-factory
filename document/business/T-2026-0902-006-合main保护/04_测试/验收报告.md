# 验收报告:T-2026-0902-006 loop 合 main 走受保护路径(gitguard 守卫)

## 环境

- Windows(pwsh)/git 2.53.0.windows.2 / Python 3.11.5 / pytest 9.1.1;
  ai-factory `main` @ 152963f(合 main 守卫已合),`origin/main` 95bcfea 之后 3 个未推送提交,
  均为 loop/工单白名单签名(152963f `merge(S1:` / a009743 `feat(T-` / 1740571 `docs(T-`)。
- 执行方式:黑盒——逐条执行验收命令与场景构造,**不读实现代码,未改任何源文件**;
  负向场景用可逆探针(临时探针文件/临时分支),执行完毕回收,事后 `git status` 干净、`main` 分支归位。

## 范围

S1:gitguard 守卫(白名单 + 脏树拦截)+ CLI `orc guard pre-merge` + playbook/驱动提示词接线
+ `tests/orchestrator/test_gitguard.py`。对照 PRD 验收标准 1-4 与 design Checkpoints 1-3 逐条执行。

## 用例结果

| # | 用例 | 结果 | 证据 |
|---|------|------|------|
| 1 | test_gitguard 单测(临时仓四场景:干净过 / 他人提交拦 / 脏树拦 / 白名单放) | 通过 | `python -m pytest tests/orchestrator/test_gitguard.py -q` → **20 passed in 94.27s,exit 0** |
| 2 | 实仓守卫:3 个未推送 loop 签名提交 + 树干净 | 通过 | `python -m orchestrator.daemon.cli guard pre-merge D:\workspace\ai-factory` → `guard pre-merge: PASS`,exit 0(fetch origin 后 origin/main..HEAD 仅白名单提交,不误拦) |
| 3 | 脏树拦截 | 通过 | 仓根放探针文件 `qa-dirty-probe.tmp` → `GUARD-BLOCK [dirty]`,exit 2,清单指名该文件;删除探针后复跑 exit 0 |
| 4 | 他人未推送提交拦截并指名 | 通过 | 临时分支 `qa/probe-foreign` 提交 `random: foreign probe commit by outsider` → `GUARD-BLOCK [unpushed]`,exit 2,清单含 **hash fb1ab4f / 作者 qa probe / 2026-09-02T14:48:15+08:00 / 首行**;分支已删、树复原 |
| 5 | 仅白名单(loop 签名)提交放行 | 通过 | 临时分支提交 `feat(T-2026-0902-006): whitelist probe commit (clean tree)`(树干净)→ `guard pre-merge: PASS`,exit 0;分支已删 |
| 6 | guide/驱动提示词接线文本 | 通过 | `docs/orchestrator-guide.md` L62、L65-73(「合 main 前必须先跑守卫,绿才合」)、L154 处置表(被拦先人工对齐再重试);`orchestrator/roles/prompts/dev.md` L5-9(先 `orc guard pre-merge <项目目录>`;loop_merge 审计事件 + `make_loop_merge_fields()`) |
| 7 | loop_merge 审计事件可查 | 通过(机制)/实例待首跑 | guide L85-86 接线文本在;守卫模块(含 `make_loop_merge_fields`)在树(`git ls-files` 确认);pool 事件流当前 **0 条 loop_merge 实例**——本单合 main(152963f)为手工收口,loop 实迹待首次走守卫的 loop 合 main 自然产生,非本单缺陷 |
| 8 | 全量回归(零回归) | 通过 | `python -m pytest tests/ -q` → **377 passed / 5 failed / 2 deselected**;5 failed 与基线 `95bcfea`(守卫合并前)隔离 worktree 复跑**逐一同款、同症状**(任务停在 pending/failed,验收子进程无法启动)——沙箱环境性存量(MSYS bash CreateFileMapping 被拒同类),非本单引入 |

## 结论

守卫达标:他人未推送提交、脏树必拦且清单指名;仅白名单(loop/工单签名)提交且树干净放行;
CLI 退出码语义(0=可合/2=拦)与 guide/dev 接线齐备;专项 20 用例全绿;全量 377 绿 + 5 项
基线同款环境性存量(附隔离复跑证据),零回归。**放行**。
