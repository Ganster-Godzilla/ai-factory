# T-2026-0903-010 发布记录:tasks 裸串双层防御

## 合并清单

| 文件 | 变更 |
|---|---|
| orchestrator/daemon/ticket.py | +`_validate_tasks`;save_ticket 收口拒入裸串 |
| orchestrator/dashboard/templates/ticket.html | tasks 循环 mapping 分支,str 项降级显示 |
| tests/orchestrator/test_dashboard.py | +3 回归用例(str 200 / 拒入 / dict 通过) |
| document/business/T-2026-0903-010-tasks裸串防御/ | 提案/PRD/设计/验收/发布全套 |

提交:`24d32d3`(main,已推 origin,guard pre-merge PASS)。

## 版本

无独立版本号;main @ 24d32d3 即发布单元。

## 部署清单

本项目(ai-factory 编排器)未登记 deploy 脚本,显式声明:部署 = main 代码生效
+ Dashboard 进程重启加载新模板。已于 2026-09-03 重启 8321 端口 Dashboard
(后台任务 busvwugk4)。pool 数据零迁移(007/008 脏数据已于立案前修复)。

## 冒烟结果

- `pytest tests/orchestrator/`:**420 passed, 2 deselected**(含 3 新用例)
- 全池 yaml 扫描:**CLEAN**,无 str tasks
- Dashboard 冒烟:007 / 008 / 010 详情页均 **200**
- save_ticket 拒入行为由单测锁定(ValueError,"tasks 契约违例")

## 回滚方案

纯增量改动,无数据迁移、无契约变更:

```
git revert 24d32d3 && git push origin main
# 重启 Dashboard 加载旧模板
```

回退后详情页对裸串恢复 500(旧行为),但全池已 CLEAN,无实际影响面;
回归用例随 revert 一并撤除,不产生悬挂测试。
