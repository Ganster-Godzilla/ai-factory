# T-2026-0903-010 验收报告:tasks 裸串双层防御

## 环境

- Windows 11 + Git Bash;Python 3.11;ai-factory 仓根
- 执行期:2026-09-03;工单 p3_running → p4_verifying
- 回归基线:`python -m pytest tests/orchestrator/`(含新增 3 用例)

## 范围

- S1 写入层校验:`orchestrator/daemon/ticket.py` `_validate_tasks` + `save_ticket` 收口
- S2 展示层容错:`orchestrator/dashboard/templates/ticket.html` mapping 分支
- S3 回归:`tests/orchestrator/test_dashboard.py` 3 新用例 + 全池扫描 + 详情页冒烟
- 契约不变更、无数据迁移(007/008 脏数据已于本工单立案前修复)

## 用例结果

| 用例 | 预期 | 实际 | 结论 |
|---|---|---|---|
| test_ticket_detail_str_tasks_200 | str tasks 工单详情 200,含串与"非法任务项"提示 | 200,两者均在响应体 | ✅ |
| test_save_ticket_rejects_str_tasks | str 项落盘抛 ValueError | ValueError("tasks[0] 契约违例...") | ✅ |
| test_save_ticket_accepts_dict_tasks | dict tasks 正常落盘回读 | 回读 id=S1 一致 | ✅ |
| 全套回归 | 既有用例不回退 | 420 passed, 2 deselected | ✅ |
| 全池扫描 | 无 str tasks | CLEAN(含 007/008) | ✅ |
| 详情页冒烟 | 007/008/010 → 200 | 200 / 200 / 200 | ✅ |

## 结论

验收标准 4 条全过,**通过**。双层防御就位:写入层快速失败(str 拒入),
展示层降级显示(存量脏数据 200 可见可诊断)。

遗留:T-2026-0903-008 的 S6(审核降级)未灌入,归属 008 自身流程,与本单无关。
