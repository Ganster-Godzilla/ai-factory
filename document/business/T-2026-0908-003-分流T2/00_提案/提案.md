# 提案-T-2026-0908-003:分流 T2——level→模型路由 + R1 文本直切 + 金丝雀台账

> Phase 0 动议。分流减员计划 T2 单;前置 T-2026-0908-001(网关三通道,已上线)。

## 问题

T1 让网关认识了三通道,但**没有任何调用方会把模型名递过去**:runner 建的 packet.model
只服务 dsh 路径,claude_code 适配器从不传 --model,relay 的 k2.6 路由组接不到流量;
编排器工单仍全部落在 kimi 付费池。同时 deepseek 文本层(ds-flash 是付费兜底,
本周已烧 149M)还没换成免费的 zy-ds;W1 金丝雀周也没有度量台账。

## 方向

1. **level→驾驶员注入**:runner 在两处建 packet 处按 ticket.level 解析驾驶员模型
   (L2/L3→'k2.6',L1→None=现默认);claude_code 适配器加 --model 白名单传递
   (只认 relay 路由组键 k2.6/gpt-6-astra,其余忽略,防 dsh 语义模型误注)。
2. **R1 文本直切**:zy-ds 从 routeOnly 移入缺省链(ds-flash 正前方)——kimi 池冷却后
   deepseek 层先落免费渠道,官方 ds-flash 留作备份;模型族不变(都是 v4-flash),
   无降级语义。zy-k2/zy-gpt 保持 routeOnly。
3. **金丝雀台账**:`scripts/canary-report.py` 周度聚合(工单×级别×驾驶员×一次通过率
   ×返工×consult + relay /__stats 渠道用量 + 控制台读数手填位),W1 每晚一分钟出表。
4. **W1 手册**:docs/orchestrator-guide.md 增分流段(自动路由口径/交互式切 k2.6 姿势/
   台账节奏/回退姿势)。

## 范围(粗略)

| # | 改动 | 文件 |
|---|---|---|
| 1 | _driver_for_level + 两注入点 + --model 白名单 | runner.py, claude_code.py, tests |
| 2 | zy-ds 移入缺省链(ds-flash 前)+ 名单/顺位/路由测试更新 | D:\Tool\kimi-relay.js, test/* |
| 3 | canary-report.py 台账脚本 + 自验 | scripts/canary-report.py |
| 4 | 分流/W1 手册段 | docs/orchestrator-guide.md |

## 不做

- 不动 dsh 适配器与 ds 现金闸语义(dsh 仍是付费兜底路径,直切发生在 relay 层)
- 不做 T3 周报复盘/T4 gpt-6 攻坚 A/B/T5 退订执行
- 不改 level 定级本身(006 已交付);不碰 T-2026-0908-002(它线工单)
- 不做 Claude Code 全局指向强切(交互式切不换 boss 默认环境,手册给自选姿势)

## 自估

跨 runner/adapter/relay 三处但均为小改;触运行路径,回滚容易(revert)。定级 L1。
