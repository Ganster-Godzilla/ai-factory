# T-2026-0908-003 发布记录:分流 T2(level→模型路由 + R1 直切 + 金丝雀台账)

## 合并清单

| 位置 | 文件 | 变更 |
|---|---|---|
| ai-factory(`0236278`) | orchestrator/daemon/runner.py | +`_driver_for_level`;两 packet 注入点 |
| 同上 | orchestrator/adapters/claude_code.py | DRIVER_MODELS 白名单传 --model |
| 同上 | tests/orchestrator/test_level.py / test_claude_code_adapter.py | +5 用例(映射/白名单/端到端注入) |
| 同上 | scripts/canary-report.py(新) | 金丝雀周度台账(只读) |
| 同上 | docs/orchestrator-guide.md | +"分流与 W1 金丝雀"小节 |
| D:\Tool(`e327a26`) | kimi-relay.js | zy-ds 移入缺省链(glm 后 ds 前);zy-k2/zy-gpt 保持 routeOnly |
| 同上 | test/relay-routes.test.js / relay-zen.test.js | 缺省链八层期望同步 |
| ai-factory 仓 | document/business/T-2026-0908-003-分流T2/ | 提案/P1/P2/验收/发布全套 |

## 版本

ai-factory main @ 见合并 commit;D:\Tool @ `e327a26`(生产 relay 已加载)。

## 部署清单

- relay:未登记 deploy 脚本,显式声明——D:\Tool 提交 + 生产 relay 重启(wscript)已执行,
  缺省链八层在线(zy-ds 在 glm-flash 后、ds-flash 前)
- ai-factory:main 代码生效;runner/adapter 改动由**下一次 runner 派发**自然生效,无进程须重启
  (dashboard 只读 level 字段,8321 已在 T1 重启后运行新版)
- keys 零变更;orchestrator.yaml 零变更;pool 数据零迁移

## 冒烟结果

- pytest tests/ 全量:**456 passed, 2 deselected**(681s)
- node --test:**30/30 绿**;pyflakes 改动文件零告警
- 生产 relay /__status:缺省链八层 = [kimi1-4, glm-flash, zy-ds, ds-flash, zen-k3],无冷却
- canary-report.py 实跑:0907-002 一次通过率 3/3,四张 0908 单在列,relay 读数正常
- 端到端(单测级):L3 单 _dispatch → packet.model='k2.6';L1 单 = 现值零变化

## 回滚方案

```
cd /d/Tool && git revert e327a26 && taskkill //PID <8787> //F && wscript kimi-relay.vbs   # zy-ds 回 routeOnly
cd /d/workspace/ai-factory && git revert <merge-commit>   # level 注入/白名单/台账消失,全回现默认
```

两仓改动解耦,可单独回滚;L1 全路径在任何回滚组合下行为不变。
