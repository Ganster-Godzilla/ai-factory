# 歧义澄清记录 — T-2026-0908-003

| # | 问题 | 结论 |
|---|---|---|
| Q1 | --model 为什么用白名单而不是透传 packet.model? | packet.model 还服务 dsh 路径(deepseek-v4-flash 等),透传会把 dsh 语义模型注给 claude CLI→kimi 上游收到不认识的模型名。白名单只认 relay 路由组键,误注不可能 |
| Q2 | zy-ds 入缺省链违背 T1"zy 不进缺省链"的设计吗? | 不违背。T1 防的是"降级"(k3→k2.6/gpt);zy-ds 与 ds-flash 同模型族(v4-flash),是同模型的免费通道,不是降级 |
| Q3 | L2 工单也给 k2.6? | 给。分流方案:L3 粗活+L2 小特性都是 k2.6 试开区;L2 裁剪流程未落地前,L2 单流程同 L1 但驾驶员已是 k2.6——这正是金丝雀要验的 |
| Q4 | 交互式 Claude Code 要强切 k2.6 吗? | 不强切。编排工单自动路由;boss 交互式环境保持默认(k3),想试 k2.6 用 --model k2.6 单次起,手册给姿势 |
| Q5 | 台账的"驾驶员"怎么认定? | 按 level 映射(L2/L3→k2.6,L1→k3),与 R1 注入同口径;真实落点以 relay /__stats 为准(冷却掉链时按实际后端记) |

无未决歧义。
