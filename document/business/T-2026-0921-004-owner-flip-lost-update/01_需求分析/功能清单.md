# T-2026-0921-004 功能清单

## 功能清单

| 编号 | 功能 | 说明 | 优先级 |
|---|---|---|---|
| F1 | transition() CAS 硬门 | 按票文件锁内重载,期望 frm 与盘上不符 → StaleTicketError;事件留痕 | P0 |
| F2 | save_ticket 全写路径同锁 | 一切写票路径过同一把锁(消除旁路直写) | P0 |
| F3 | Dashboard 单实例 PID 锁 | 启动活锁校验拒启+打印占用 PID;退出清锁;死锁可回收 | P0 |
| F4 | 回归测试 | 双写竞态响亮失败+yaml 零污染;单实例拒启/回收;既有套件全绿 | P0 |

优先级口径:P0=病灶(沉默覆盖)不除事故必复现,无 P1 可延项。

## 词汇

- lost-update=两进程各持一票对象先后保存,后写覆盖先写的字段级丢失
- CAS=compare-and-swap,此处语义=锁内重载比对期望状态,不符即拒
