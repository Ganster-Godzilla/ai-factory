# T-2026-0921-004 验收报告:批准边 owner 翻转不落库(lost-update)根治

## 环境

- ai-factory 编排器:pytest tests/ 全量(回归基线=CAS 落地前 460/460 orchestrator 目录)
- 切片:O1(ticket.py CAS)/O2(runner stale 降级)/O3(Dashboard PID 锁)/O4(本报告)

## 范围

PRD F1-F4:transition()/save_ticket CAS 硬门(按票锁+内容指纹,StaleTicketError
响亮拒绝);runner 存点 stale 降级(stale_aborted 事件+弃写不续跑);
Dashboard 单实例 PID 锁(活拒启/死回收/退清锁);回归测试。

## 用例结果(PRD O1-O4 逐条)

| # | 场景 | 证据 | 判定 |
|---|---|---|---|
| O1 | CAS 硬门:持旧对象写响亮失败,yaml 零污染 | tests/orchestrator/test_ticket_cas.py 8 例:双对象同载先后存→第二个 StaleTicketError+yaml 保持先写内容;文件被删后持旧对象保存也拒 | 通过 |
| O1b | 连存不误伤 | 同对象 load→save→改→save 全过;fresh load 后可写;指纹不落盘(yaml 无 _disk_fp 键) | 通过 |
| O2 | 正常路径不破 | orchestrator 目录 460/460 绿(CAS 引入后首轮回归);test_runner_stale 4 例:boss 点击竞态→stale_aborted 事件+弃写+零污染,无竞态正常跑不受影响 | 通过 |
| O3 | 单实例三态 | test_dashboard_pidlock 8 例:活锁拒启打印占用 PID/死锁回收+pid_lock_reclaimed 事件/release 只清自己的锁;同 PID 重入幂等(进程内二次启动非孤儿,cli 冒烟实证修复) | 通过 |
| O4 | 留痕 | stale_aborted/pid_lock_reclaimed 均入事件流可查 | 通过 |

## 根因回顾(与 PRD 对账)

- 直接原因:事发四次批准事件无 owner_flipped 字段=点击打到旧代码孤儿进程——
  由 O3 PID 锁根治(活锁拒启,孤儿无法双绑受理)
- 系统缺陷:save_ticket 无并发保护,旧对象静默回写——由 O1 CAS 根治
  (沉默覆盖变 StaleTicketError 响亮失败)+O2 降级(runner 竞态弃写留痕)
- 副产物修复:同 PID 重入误拒(cli 冒烟二调自撞)→幂等放行,测试在册

## 结论

PRD O1-O4 逐条有实证,全套件 487/487 绿(2 deselected 为既有标记)——**验收通过**。
