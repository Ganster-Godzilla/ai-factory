# T-2026-0921-004 发布记录:owner 翻转不落库(lost-update)根治

## 合并清单

ai-factory(main→origin/main):
- O1 CAS 硬门:ticket.py 内容指纹(_disk_fp)+按票锁+StaleTicketError;test_ticket_cas.py 8 例
- O2 runner stale 降级:advance_once 捕获+stale_aborted 事件+弃写;test_runner_stale.py 4 例
- O3 Dashboard PID 锁:app.py 三态+cli 启动钩;test_dashboard_pidlock.py 8 例
- 同 PID 重入幂等修复(cli 冒烟实证)+pyflakes 清理
- docs:P1/P2/验收报告(QA 黑盒复跑留痕)/交付说明/本记录
- 搭车 housekeeping:orchestrator.yaml 登记 ai-lab;backlog 核销 0919-011

## 版本

- ai-factory:v1.3.1(已推 origin/main + tag);全套件 487/487 绿(QA 复跑 595s;发布前终轮 761s)

## 部署清单

- 纯代码增量,无数据迁移;pool 锁文件(<id>.lock/.dashboard.pid)运行时自产自清
- **重启 Dashboard 生效 PID 锁**:首个新实例启动自动登记;若报"已在运行(PID=n)",
  确认 n 已死后删 pool/.dashboard.pid 或直接重启(死锁自动回收)
- runner/CLI 下次调用自然走 CAS(行为变化见交付说明"运维须知")
- 发布执行(2026-09-24T03:55Z):dashboard 重启(旧 pid 19472 → 新 pid 13948;
  首轮 43596 → 19472 亦成功,记录去重保留终轮);
  PID 锁实证:deploy 重启全程未触发"已在运行"拒启,旧实例死锁自动回收正常

## 冒烟结果

- 全套件 487/487 绿(2026-09-24 终轮实跑+QA 黑盒复跑一致)
- cli dashboard 冒烟(进程内二次启动)通过(同 PID 重入幂等)
- 生产 pool 全部 yaml 无 _disk_fp 键(QA grep 核验)
- `http://127.0.0.1:8765/` → 200(5909ms)→ PASS;**总体判定:全绿,发布成功**

## 回滚方案

- 三切片均纯增量:revert O1/O2/O3 对应提交即回旧行为(不建议=请回沉默覆盖)
- PID 锁故障兜底:删 pool/.dashboard.pid 即可启动(运维手册已记)
- git tag 回切:`git checkout <上一 tag>`;重启 dashboard:
  `python -m orchestrator.daemon.cli dashboard --port 8765`;按冒烟清单复验全 200 才算回滚成功

