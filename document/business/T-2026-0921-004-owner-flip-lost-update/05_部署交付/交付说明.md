# T-2026-0921-004 交付说明:owner 翻转不落库(lost-update)根治

## 交付物

| 切片 | 产物 | 说明 |
|---|---|---|
| O1 | orchestrator/daemon/ticket.py + test_ticket_cas.py(8 例) | CAS 硬门:内容指纹(_disk_fp,内存私有)+按票锁;StaleTicketError |
| O2 | orchestrator/daemon/runner.py + test_runner_stale.py(4 例) | advance_once stale 降级:事件+弃写不续跑 |
| O3 | orchestrator/dashboard/app.py + cli.py + test_dashboard_pidlock.py(8 例) | PID 锁三态+同 PID 重入幂等;接入 cli dashboard 启动钩 |

## 行为变化(运维须知)

1. **持旧对象的写会炸**:任何代码 load 工单后,若盘上被他人写过再保存,
   抛 StaleTicketError——这是设计(既往那是沉默损坏)。遇到=重新 load 再写。
2. **Dashboard 第二个实例拒启**:报"已在运行(PID=n)"。确认 n 是死进程后,
   删 pool/.dashboard.pid 或直接重启(死锁自动回收)。
3. **runner 竞态弃写**:boss 点击与 runner 写入撞车时,runner 弃写并留
   stale_aborted 事件;下一轮 advance 自然按新状态推进,无需人工干预。

## 回滚

三切片均纯增量:CAS 比对三行、PID 锁两函数、runner 一个捕获块——
revert 对应提交即回旧行为(不建议:等于请回沉默覆盖)。
