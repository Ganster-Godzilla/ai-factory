# 歧义澄清记录:复杂度闸接入常设质量闸

- 工单:`pool/tickets/T-2026-0903-003.yaml`

## 已澄清

| 问题 | 结论 | 依据 |
|---|---|---|
| 用 ruff 还是 radon? | radon cc | ruff 的 C901 阈值是布尔判定;radon 给精确 cc 数值,恶化拦截(升高即拦)需要数值比较 |
| 阈值? | cc > 15(C 级以上)拦 | 002 拆分后各函数 ≤15;C 级是"开始难维护"的公认分界 |
| 存量烂函数怎么办? | 冻结进基线豁免,本闸不强制拆(那是 002 类单的事);但恶化(cc 升高)仍拦 | 提案"不做"首条 |
| radon 放哪? | requirements.txt(与 pytest 同级,检查工具先例) | 无独立 dev 依赖文件;pytest 亦 dev 工具在列 |
| 接进哪个守卫? | 合 main 前与 check-pool-load 同跑(手动/文档约定);暂无远端 CI 不接 | stack-profile ci.type=none |
| 无 radon 环境? | 响亮报错+提示安装,非静默放行(防"闸空转",R7/规则#9 教训) | AC5 |

## 待澄清(设计期定)

- 基线文件格式:`<相对路径>::<函数限定名>::<cc>` 一行一个,易读易 diff;
  精确格式设计期定(须能定位到函数且支持 cc 数值比较)。

## 无其余歧义

边界(只闸圈复杂度、只拦增量/恶化、不接 ruff 全规则集)无歧义。
