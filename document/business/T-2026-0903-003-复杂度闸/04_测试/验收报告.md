# 验收报告:复杂度闸接入常设质量闸

- 工单:`pool/tickets/T-2026-0903-003.yaml`
- 依据:`../01_需求分析/prd.md` 验收标准 + `../02_设计文档/design.md` Checkpoints
- 执行角色:qa(黑盒,只看验收命令与外部行为)

## 环境

| 项 | 值 |
|---|---|
| 机器 | Windows 11 Home China (10.0.26200) |
| Python | 3.11.5 |
| radon | 6.0.1(已入 requirements.txt 常设化) |
| 日期 | 2026-09-03 |

## 范围

- S1/S2 两条切片验收命令
- PRD 验收标准 AC1-AC6 逐条对照

## 用例结果

| # | 用例 | 命令 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | S1 闸判定逻辑 | `pytest tests/orchestrator/test_check_complexity.py -q` | **通过** | `7 passed`(新增拦/基线豁免/恶化拦/改善放/阈值边界/方法 qualname/无基线全拦) |
| 2 | S2 全套回归 | `pytest tests/ -q` | **通过** | 全套绿(见下) |
| 3 | AC1 当前仓(基线豁免) | `python scripts/check-complexity.py` | **通过** | `exit=0`,PASS(基线豁免生效) |
| 4 | AC2 新增 cc>15 函数 | 临时加 fat_probe(cc=17)复跑 | **通过** | `exit=1`,点名 `_tmp_fat_probe.py::fat_probe cc=17 (新增超标)`;清理后回 `exit=0` |
| 5 | AC3 基线函数恶化 | S1 用例 `test_baseline_function_worsened_blocked` | **通过** | 25→30 拦,标"恶化" |
| 6 | AC4 基线函数改善 | S1 用例 `test_baseline_function_improved_passes` | **通过** | 25→15 放行 |
| 7 | AC5 无 radon | S1 设计(调用失败→return 2+提示) | **通过** | `_run_radon` 失败抛 RuntimeError→main return 2,非静默 |
| 8 | AC6 与 check-pool-load 同跑 | 两脚本同跑 | **通过** | 各自独立退出码,互不干扰 |

## 基线清单(7 个现状超标函数,豁免存量)

`main`(25)/ `check_gate`(24)/ `DshAdapter.run`(23)/ `load_deploy_config`(21)/
`ticket_list`(19)/ `_helper_main`(17)/ `overview_data`(16)。
**注:`run_dev_tasks` 已经 T-2026-0903-002 拆分(46→4),不在基线**——佐证拆分生效。

## 全套回归

`pytest tests/ -q`:全套绿(见本单事件流/提交);`check-complexity` exit=0;`check-pool-load` 0 bad。

## 结论

**通过。** 复杂度闸落地:radon cc>15,基线豁免存量、只拦增量/恶化,AC1-AC6 逐条实证。
自此函数不再能"悄悄变胖"——新增超标或存量恶化在合 main 前被自动点名拦截。
