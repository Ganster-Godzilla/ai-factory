# 歧义澄清记录

- 模型:zen-k3 固定重写为 kimi-k3(boss 指定),客户端 model 字段忽略。
- 流式:不上游流式(翻译窗口期最简可靠);下游 SSE 为合成,时延=上游全量返回时间,可接受(兜底场景)。
- kimi-k3 reasoning 输出(reasoning_tokens)不进 Anthropic thinking 块,丢弃——保文字与工具调用。
- 工具:仅覆盖 claude CLI 常规子集(tools 声明、tool_use、tool_result);computer use 等高级块不在范围。
- 其余:无歧义。
