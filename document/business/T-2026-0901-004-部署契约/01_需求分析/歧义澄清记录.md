# 歧义澄清记录

## 待 boss 决策(阻塞实现,不阻塞需求)

1. **deploy 凭证保管方式**:SSH key 已有(~/.ssh/sk-aliyun-47);sudo 密码怎么管——
   候选:(a) deploy 用户 sudoers 白名单(`systemctl restart sk-api` 单条 NOPASSWD,推荐);
   (b) 服务改 User=deploy 免 sudo;(c) boss 每次现场输入;(d) 凭据文件 600 权限。
   2026-09-01 工单摘要明确此项需 boss 决策,需求侧按(a)/(b) 二选一写契约条款。

## 已澄清

- **local 与 remote 的地位**:local 是一等公民而非 remote 降级——boss 2026-09-01 明确
  "没有云服务器时 CI/CD 就是本地化",已写入提案方向。
- **venv 新依赖**:服务器不能现场装依赖——依赖变更必须在 PRD/发布单阶段声明;
  需要新依赖时发布单含 venv 重建步骤(本机构建上传)。首次部署实战已验证。
- **uid 穿透**:发布脚本必须 chown 归一(或 tar --owner),已并入 F2 验收口径。
- **nginx**:已有 SPA try_files,前端新增路由免 nginx 变更,属部署常识(F10),不算歧义。

## 其余

- 无歧义。
