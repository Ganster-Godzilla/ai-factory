# 功能清单

| # | 功能 | 优先级 | 说明 |
|---|------|--------|------|
| F1 | deploy target 抽象(local/remote 双形态) | P0 | local 一等公民;项目登记 deploy 配置(target/主机/服务名/冒烟清单) |
| F2 | deploy 步骤脚本化 | P0 | remote:本地构建 dist → tar+ssh → 解包留版 → 重启;uid 归一(chown);依赖差异前置检查 |
| F3 | 部署后健康检查冒烟 | P0 | /api/health + 关键新路由 + 前端入口全 200 才算成;结果留痕 |
| F4 | 回滚说明 | P0 | 上一版包留存 + 回切步骤进发布记录;local 走 git tag 回切 + 重启 |
| F5 | 冒烟失败自动 incident + 不静默放行 | P0 | 失败即建工单,回滚说明随附 |
| F6 | .env 管理约定 | P0 | 密钥不落库;.env.example vs 服务器 .env 差异检查进冒烟,差异清单进部署单 |
| F7 | local 形态:合并后长驻进程自动重启 | P0 | 治"合并后忘重启"病根;重启清单按项目登记 |
| F8 | sudo 卡点处置条款 | P0 | sudoers 单条 NOPASSWD 白名单 或 服务改 User=deploy,契约二选一(待 boss 决策凭证保管) |
| F9 | 双形态试点演示 | P1 | local=ai-factory 自身,remote=sk-video-studio,各一次全链路 |
| F10 | 部署常识沉淀(nginx SPA 路由免改等) | P1 | 写入部署文档,前端新增路由零 nginx 变更 |
| F11 | 通用 CI/CD 平台 / 多环境分级 | P2 不做 | 本工单非目标 |
