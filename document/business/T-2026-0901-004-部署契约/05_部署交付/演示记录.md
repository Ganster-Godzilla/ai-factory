# 演示记录:T-2026-0901-004(G7 本地形态 + 失败路径演示 + G8 远程形态演示)

本记录为 G7 验收留痕(design Checkpoint 3):**local 形态演示**(CLI 重启 dashboard + 本地冒烟全绿)与**失败路径演示**(登记 404 冒烟 URL → incident 自动建成 + 回滚说明可照抄)。演示于 2026-09-01 真实执行(dogfood 自证),以下命令、退出码、冒烟结果、事故单与事件均来自实际运行,非示例文本。

## 本地演示

### 环境与前提

- 演示在 G7 worktree(`.orc-worktrees/T-2026-0901-004-G7`)内完成:沙箱限写主仓 checkout,故 `projects.ai-factory` 在演示期间临时指向本 worktree,演示后已还原为 `d:/workspace/ai-factory`(分支最终 diff 仅保留 start_cmd 修复,见下)。
- 演示池 = 真实 `pool/` 的完整副本(本 worktree `pool/`,gitignored,22 张 yaml 工单 + 原单事件流,含原单 T-2026-0901-004 事件);**真实系统池不受演示影响**。
- **start_cmd 修复(本分支交付)**:原登记 `python -m orchestrator.dashboard.app` 没有 `__main__` 入口,`orchestrator/dashboard/app.py` 只提供 `create_app`,拉起即空转、冒烟必挂;修复为 `python -m orchestrator.daemon.cli dashboard --port 8765`(`cli.py` 的 dashboard 子命令导入 create_app 并以 `--port` 拉起,与 `tests/orchestrator/test_deploy_local.py` 实证命令一致)。本次演示即验证修复后 dashboard 可真实启动并冒烟 200。

### 执行与输出

命令(worktree 根目录):

```
python -m orchestrator.daemon.deploy T-2026-0901-004
```

退出码:**0(全绿,发布成功)**

部署清单(发布记录实况,`git describe --tags --always` → `ecf2447`):

- 版本:`ecf2447`
- 目标:`local`
- 时间:`2026-09-01T06:03:02+00:00`
- 重启进程:dashboard(首启 → 新 pid 46748)

冒烟结果(发布记录实况):

- `http://127.0.0.1:8765/` → 200, 3813ms → PASS
- 总体判定:**全绿,发布成功**

dashboard 日志实证(`.orc-local/dashboard.log`):

```
 * Running on http://127.0.0.1:8765
127.0.0.1 - - [01/Sep/2026 14:03:02] "GET / HTTP/1.1" 200 -
```

### pidfile 重启路径(同进程连跑两次)

同一命令行内连续执行两次 deploy,第二次读到 pidfile 旧 pid 并 terminate 后重启(发布记录实况):

- 第 1 次:dashboard(首启 → 新 pid 46748)
- 第 2 次:dashboard(旧 pid 46748 → 新 pid 45736);46748 为活进程,真实 terminate → 重启
- 两次均退出 0、冒烟全绿(`200, 3699ms → PASS`)→ 证明"合并后忘重启"由脚本兜底,重启动作不靠人记得

### 留痕与证据

- 发布记录:`05_部署交付/发布记录.md`(部署清单 / 冒烟结果 / 回滚方案三章;内容上文已引)。演示后已清理该文件与 `.orc-local/`,避免以 p3 工单预置 P5 发布记录、污染正式发布流。
- 冒烟全绿以 exit 0 + 发布记录章节双重留痕。

### 结论

local 形态演示通过:CLI 一键完成"重启长驻进程 + 本地冒烟 + 发布记录三章",退出 0 = 全绿;dashboard 冒烟 200 留痕。

## 失败路径演示

### 场景登记

向部署配置 `deploy.ai-factory` 的 smoke 清单临时登记一个 404 URL:

```
smoke: ["http://127.0.0.1:8765/", "http://127.0.0.1:8765/__smoke_404_demo__"]
```

(演示后已还原,分支最终 diff 不含该 URL。)

### 执行与输出

命令(worktree 根目录,同一 CLI):

```
python -m orchestrator.daemon.deploy T-2026-0901-004
```

退出码:**1(EXIT_DEPLOY_FAILED,发布失败不静默放行)**

冒烟结果(发布记录实况):

- `http://127.0.0.1:8765/` → 200, 3775ms → PASS
- `http://127.0.0.1:8765/__smoke_404_demo__` → 404, 6030ms → FAIL
- 总体判定:**存在失败,发布失败**

### 事故单自动建成

失败处置自动执行:`new_ticket(type=incident, related_ticket=原单)` + 原单事件流 `incident_created` 回链。

事故单(演示池副本,可复核 `pool/tickets/T-2026-0901-006.yaml`):

- id:`T-2026-0901-006`
- type:`incident` / project:`ai-factory` / state:`p1_drafting` / owner_role:`pm` / priority:`high`
- summary:`部署/冒烟失败:T-2026-0901-004 release 契约补部署环节(CI/CD 缺口治本)…`(自动拼接原单摘要)
- related_ticket:`T-2026-0901-004`
- created_by:`system` / created_at:`2026-09-01T06:05:21`

回链事件(`T-2026-0901-004.events.jsonl` 追加,实况):

```json
{"ts": "2026-09-01T06:05:21.060319+00:00", "ticket": "T-2026-0901-004", "actor": "system", "event": "incident_created", "incident": "T-2026-0901-006"}
```

### 回滚说明(可照抄)

发布记录"回滚方案"章节由脚本自动生成(local 形态),命令可照抄执行:

```
1. git tag 回切(命令可照抄):`git checkout <上一 tag>`
2. 重启长驻进程(逐条可照抄):
   - dashboard:`python -m orchestrator.daemon.cli dashboard --port 8765`
3. 按部署冒烟清单复验,全 200 才算回滚成功
```

注:版本未知时以 `<上一 tag>` 占位;当前部署版本为 `ecf2447`,回滚目标即上一发布版本。

### 还原验证

移除 404 URL、还原 smoke 清单后再次执行同一命令 → 退出码 **0(全绿,发布成功)**;发布记录实况 `dashboard(旧 pid 45868 → 新 pid 47840)`、`200, 3780ms → PASS`。证明失败确由登记的 404 冒烟 URL 触发,非环境噪声。

### 结论

失败路径演示通过:任一冒烟 URL 非 200 → 自动建 incident 工单(related_ticket 双向可查)+ 原单回链事件 + 发布记录写失败详情与回滚说明 + 非零退出(不静默放行);回滚说明可照抄执行。

## 演示说明(环境差异与后续)

- 沙箱在单条命令结束时回收子进程,故演示中每次冒烟均在部署进程内完成(200/404 均已留痕);正式环境由 `CREATE_NEW_PROCESS_GROUP` 脱离会话,长驻进程不随命令退出。
- 事故单 `T-2026-0901-006` 建在演示池副本(gitignored),供 boss 复核;真实系统池未受演示影响。
- 回归状态:本切片代码零改动,`pytest tests/` = 353 passed / 5 failed / 2 deselected;5 个失败均为存量测试在沙箱内的环境性失败(沙箱拒绝 cygwin bash 的 CreateFileMapping,凡 `acceptance_cmd` 经 `bash -c` 执行的测试即失败,HEAD 基线同款失败,与本切片无关),完整回归需在宿主/CI 环境执行。
- 演示产出的 `发布记录.md` 与 `.orc-local/` 已清理;`deploy.py` 注释声称 `.orc-local` 已被 gitignore,但 `.gitignore` 未登记该目录(仅 `*.local`,不匹配 `.orc-local`)——建议后续任务补登(超出本任务 scope)。
- 远程形态演示与对应冒烟留痕由 G8 后续追加(本文件预留扩展)。

## 远程演示

### 前置:restart_cmd 凭证形态(形态 a)的决策依据与落地

- **决策依据**:`01_需求分析/歧义澄清记录.md` item 1 候选 (a)"deploy 用户 sudoers 白名单(`systemctl restart sk-api` 单条 NOPASSWD,推荐)"为登记方向;`00_提案/提案.md` L17 补记(2026-09-01 部署收官)要求契约收编必须回改 **sudoers 单条 NOPASSWD + PermitRootLogin no**。`orchestrator.yaml` 的 `restart_cmd` 已按 (a) 登记为 `sudo -n systemctl restart sk-api`。G8 按 (a) 落地并留痕;注:决策依据为文档已登记方向(2026-09-01 工单摘要明示此项需 boss 决策),无独立 consult 事件留痕,凭证保管方式仍按提案"不替 boss 决策"不越权。
- **落地(服务器 47.109.84.154,2026-09-01)**:
  - `/etc/sudoers.d/sk-api-restart`(单条,`deploy ALL=(root) NOPASSWD: /usr/bin/systemctl restart sk-api`,chmod 440)+ `visudo -c` 全部 parsed OK;
  - 部署完成后回改:`/etc/ssh/sshd_config` `PermitRootLogin yes` → `no` → `sshd -t` OK → `systemctl reload sshd`;实测 **root 通道拒绝**(`Permission denied (publickey,password)`)、**deploy 通道可用**(`sudo -n systemctl restart sk-api` → active,pid 106411)。契约既定终态达成。

### 环境与拓扑收编

- 服务器 app_dir 实测为 `/data/apps/sk-video-studio`(配置原登记 `/opt/sk-video-studio` 有误,G8 已校正 `orchestrator.yaml` 两处:`app_dir` 与 `remote_env`);systemd unit `sk-api`、nginx `sites-available/sk`(sites-enabled 软链)。
- **拓扑收编(零停机 bootstrap)**:`current` 自指软链(`ln -sfn . current`)→ unit `WorkingDirectory=current/apps/api`、nginx `root current/apps/web/dist`(daemon-reload / nginx -t + reload 通过)→ 首个流水线部署原子翻转 `current` 到 `releases/<ver>`,服务即切新代码。部署前旧代码仍由 `current→.` 兜底,nginx 服务不变。

### 执行方式与 CLI 的差异(如实说明)

- 本单自身 `project=ai-factory`(平台仓),远程演示目标为 `sk-video-studio`;驱动脚本以 `dataclasses.replace` **仅覆写 `ticket.project`**,`run_deploy` 本体零改动,与 `python -m orchestrator.daemon.deploy <tid>` 同一入口语义。
- `project_dir` 用**精简暂存区**(整仓剔除 `.orc-worktrees/`、`.runtime/`、`apps/api/.venv`、`.env` 等,≈79MB vs 整仓 2.1GB):规避整仓快照体积与"本机 .env 落服务器"问题。**实测缺口**:流水线整仓快照无排除清单(设计即如此,靠 project_dir 取舍),建议后续在契约/文档补"project_dir 交付物清单"约定。
- **环境变通两处**(演示会话沙箱所致,代码零改动,均如实记录):
  1. 会话沙箱禁止 Node 派生带管道子进程(vite 启动时对 `net use` 的探测恰属此类,`spawn EPERM`;escalation 无审批通道)→ 以临时 preload(`g8-vite-preload.cjs`,演示后删除)将探测改为空输出成功,vite 回退 `fs.realpathSync.native`,**真实 vite 构建照常完成**(输入同一 HEAD 快照,产物等价,28.7s);
  2. 本机 locale=GBK,流水线 `subprocess` text 模式解码 npm UTF-8 输出崩溃(`UnicodeDecodeError: 'gbk' codec`)→ 以 `PYTHONUTF8=1` 运行(演示环境变量)。
- **失败尝试的 incident 留痕**(失败路径自动处置的额外实证):第 1 次(沙箱 EPERM)、第 2 次(GBK 解码)各自动建 incident `T-2026-0901-007`、`T-2026-0901-008`(type=incident / related_ticket=T-2026-0901-004 / p1_drafting)+ 原单事件流 `incident_created` 回链(演示池 `pool/tickets/`,真实池不受影响);第 3 次成功。

### 执行与输出

命令(worktree 根目录,驱动脚本):

```
python _g8_remote_demo.py
```

退出码:**0(全绿,发布成功)**;运行窗口 2026-09-01 ~15:03–15:06(CST)。

- 版本:`git describe --tags --always` → **`v0.5.0-14-g9b5f1c9`**;包体 `orc-deploy-sk-video-studio-v0.5.0-14-g9b5f1c9.tar.gz` ≈ 19.8MB(78.7MB 暂存区压缩)。
- 远端目录:`releases/v0.5.0-14-g9b5f1c9/` 解包(15:05:58 +0800,含 `apps/api`、`apps/web`、`providers.json`、`.env.example`、`comfy-models.yaml`、`workflows/`),tar.gz 已删,`.requirements.sha` 已写(65B)。
- **current 翻转与进程证据**(实况):`readlink current` → `releases/v0.5.0-14-g9b5f1c9`;`systemctl show sk-api` WorkingDirectory=`current/apps/api`,MainPID=106048;`readlink /proc/<pid>/cwd` → `/data/apps/sk-video-studio/releases/v0.5.0-14-g9b5f1c9/apps/api`(**进程确实运行 release 代码**)。
- **依赖差异**:首装远端无基准 → `--allow-deps-change`(部署单必带项,驱动传入);服务器沿用既有同款 Linux venv(`apps/api/.venv`),unit `ExecStart` 保持原 venv 路径,release 内不携带 .venv——venv 策略差异(依赖变更须 PRD/发布单声明 + 重建说明)按已澄清口径执行。
- **.env 差异(只比 key 名,值永不回传)**:服务器 .env 较 `.env.example` 多 `SK2_AD_LLM_PROVIDER`、`SK2_AD_LLM_FALLBACK_PROVIDER`;example 多出 OLLAMA / ALI / VOLC 系列与 `SK2_PROVIDERS_PATH` 等;差异仅登记,不阻断。
- **补正(必要动作)**:暂存区不含 `.env` → release 无 `.env`;部署后执行 `ln -sfn /data/apps/sk-video-studio/.env <release>/.env` 并经 deploy 通道 `sudo -n systemctl restart sk-api`(pid 106267 → 106411)恢复服务器实配。**缺口如实记入**:流水线整仓快照无排除清单,`.env` 等本机敏感/运行时文件靠 project_dir 取舍规避,建议后续补"project_dir 交付物清单"与 .env 策略文档。

## 冒烟结果

### 流水线进程内冒烟(发布成功判定)

- 清单(配置原样):`http://47.109.84.154/api/health`、`http://47.109.84.154/`
- 结果:**全 200 → 总体判定全绿,exit 0(发布成功)**

### 补正后复验(非部署机,2026-09-01 实况)

- `GET http://47.109.84.154/api/health` → **200**,body 实况:`{"services":{"comfyui":false,"ollama":false},"providers":{"alibaba-happyhorse-i2v":false,"alibaba-happyhorse-r2v":false,"alibaba-wan27-r2v":false,"volcengine-…}}`(环境宽容,缺失能力如实 false,不误报)
- `GET http://47.109.84.154/` → **200**(462B),index.html 引用 **`/assets/index-CxFpkP4D.js` + `/assets/index-CttldOfH.css`**——与流水线构建输出逐字一致,证明 nginx 服务的即 release 新构建
- `GET http://47.109.84.154/api/providers` → **200**(关键路由,不止 health)
- **冒烟两条硬规(提案 L17)**:构建时查 `VITE_API_BASE_URL` 同源——`apps/web/src/lib/api.ts` L5 注释"仅当后端与前端分离部署时才需 VITE_API_BASE_URL 绝对地址",默认同源 ✓,`vite.config.ts` 的 `/api` 代理仅 dev 生效不入构建 ✓;部署后非部署机真实浏览器验证——本次以非部署机 curl 等价验证(200 + 新资源指纹),真实浏览器人工确认建议由运营侧在发布流程执行。

### 结论

远程形态演示通过:CLI 一键完成 **构建(vite)→ 打包上传 → 远端解包 → 依赖/.env 差异检查 → current 原子翻转 → restart_cmd 重启 → 冒烟全绿 → exit 0**,服务零停机切换至 `releases/v0.5.0-14-g9b5f1c9`;restart_cmd 凭证形态 (a)(sudoers 单条 NOPASSWD + PermitRootLogin no)落地并验证;失败自动 incident、回滚方案(上一版 `ln -sfn releases/<prev> current` + 重启)沿用 G7 验证口径,可照抄。
