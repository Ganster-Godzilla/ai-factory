# 发布记录:T-2026-0901-004 release 契约补部署环节(CI/CD 缺口治本)

## 合并清单

**ai-factory(main):** 本单按规则#11「切片即合 main(loop 自动)」逐片合入 main,集成分支即 main(design:integration_branch=main),发布员复核全部交付均在 main,无遗留待合并分支。

- bbfe146 部署契约提案(本地/远程双形态,boss 2026-09-01 约束)
- 4ca3c48 提案并入首次部署实战情报五条(sudo/chown/venv/.env/nginx)
- 782adc6 需求分析三件套(P1,提案更名规范化)
- 74b6e3f 设计+切片(P2,架构师产出)
- d916623 merge(G1):deploy.py 骨架+配置 schema 加载校验(tests 23 绿)
- 8cea0e5 merge(G2):冒烟检查器+差异检查(.env 仅 key 名;requirements 哈希,tests 17 绿)
- 090b037 merge(G3):发布记录章节写入+冒烟失败自动建 incident+非零退出(tests 19 绿)
- 26702d1 merge(G4):remote 流水线+CLI __main__
- e3d7688 merge(G5):local 流水线(pidfile 重启闭环+本地冒烟)
- ecf2447 merge(G6):门禁与契约同步(artifacts P5 扩三章/SKILL 五步契约/release 提示词/runbook 五坑)
- d8e2df3 merge(G7):local 形态演示+失败路径演示(演示记录留痕,dogfood 自证)
- b572902 merge(G8):remote 形态演示(sk-video-studio 真机:构建→上传→重启→冒烟 200)
- 940d097 P4 黑盒验收报告(G1-G8 八条验收命令全绿+整体闸留痕)

**合并前全量测试(发布员实跑,2026-09-01):**

- `python -m pytest tests/` → **353 passed / 5 failed / 2 deselected**
  - 5 failed 均为沙箱环境性存量失败:acceptance_cmd 经 `bash -c` 复检,本沙箱拒绝 cygwin bash 的
    CreateFileMapping(Win32 error 5);失败用例全在 test_routing/test_e2e_circuitbreaker,
    与 runner/circuitbreaker 代码均非本单触碰(最近改动 6a54c8a/1abd09e,早于本单),
    HEAD 基线同款失败,与 P4 验收报告(940d097)记载一致;本单范围 deploy 测试全绿
  - 2 deselected = `-m "not slow"` 常规口径
- `python scripts/check-pool-load.py` → checked 23 tickets, **0 bad**(R8 防 schema 偏斜)
- `python -m pyflakes <本单改动 .py>` → 净

## 版本

- ai-factory:main @ 940d097,发布 tag **v1.0.0**(本仓首个版本 tag;state.json version=1.0)
- 部署形态:`local`(ai-factory 自身,吃狗粮)——重启 dashboard 长驻进程 + 本地冒烟 `http://127.0.0.1:8765/`
- 本单即发布契约落地:发布=合并→tag→deploy 脚本→冒烟→记录,部署执行在脚本里不靠人记得
## 部署清单

- 版本:`v1.0.0`
- 目标:`local`
- 时间:`2026-09-01T08:11:41.468309+00:00`
- 重启进程:dashboard(首启 → 新 pid 6676)

## 冒烟结果

- `http://127.0.0.1:8765/` → 200, 4572ms → PASS

**总体判定:全绿,发布成功**

## 回滚方案

1. git tag 回切(命令可照抄):`git checkout <上一 tag>`
2. 重启长驻进程(逐条可照抄):
   - dashboard:`python -m orchestrator.daemon.cli dashboard --port 8765`
3. 按部署冒烟清单复验,全 200 才算回滚成功

## 观察窗(24h)

- 看:本单交付的 deploy 契约在下一次工单 release 时自动带出(合并→tag→deploy 脚本→冒烟→记录);
  dashboard 8765 存活;check-pool-load 0 bad 保持
- 2026-09-02 由 sre 写观察窗报告,monitoring → done

