# 验收报告 — T-2026-0901-004(黑盒执行)

> 执行角色:QA(测试执行)。依据:`document/business/T-2026-0901-004-部署契约/02_设计文档/design.md`
> Checkpoints 与工单 tasks G1–G8 的 `acceptance_cmd`,逐条字面执行。
> 执行时间:2026-09-01(会话复跑)。不改任何源文件;以下全部命令在仓库根 `D:\workspace\ai-factory` 执行。

## 环境

- 系统:Windows 11 / Python 3.11.5(python.exe,C:\Users\eltrn\AppData\Local\Programs\Python\Python311)
- pytest 9.1.1;git 工作树干净(HEAD `243fec5`,T-2026-0901-004 交付已合入)
- **PYTHONPATH 准备**:命令按字面 `python -m pytest …` 执行(`-m` 注入仓库根,与编排器 runner 运行方式一致)
- **沙箱限制(影响判读)**:会话沙箱阻止 cygwin 共享内存段创建,`bash -c "exit 0"` 实测返回 rc=256
  (`CreateFileMapping … Win32 error 5`)。凡 runner 对 `acceptance_cmd` 经 `bash -c` 独立复检的
  测试在本环境必失败——与 T-2026-0828-003 验收报告、本单 G7/G8 演示记录(L130)同款环境性失败,
  HEAD 基线同款,与本切片交付无关。

## 范围

- 用例:工单 tasks G1–G8 共 8 条验收命令,逐条字面执行,记录退出码与输出。
- 整体闸:design Checkpoint 5 —— 全量 `pytest tests/` + 变更文件 pyflakes + `python scripts/check-pool-load.py`。
- **Playwright 脚本**:本计划无 Playwright 用例(全仓检索仅设计文档提及 Playwright 分流原则,
  无任何 Playwright 脚本文件),无脚本可执行。
- 不看实现代码;验收命令只验证「对外可观测行为」。

## 用例结果

| 任务 | 验收命令(字面) | 判定 | 证据 |
|---|---|---|---|
| G1 | `python -m pytest tests/orchestrator/test_deploy_config.py -q` | ✅ PASS | **23 passed** in 1.04s,exit 0 |
| G2 | `python -m pytest tests/orchestrator/test_deploy_smoke.py -q` | ✅ PASS | **17 passed** in 1.96s,exit 0 |
| G3 | `python -m pytest tests/orchestrator/test_deploy_record.py -q` | ✅ PASS | **19 passed** in 2.40s,exit 0 |
| G4 | `python -m pytest tests/orchestrator/test_deploy_remote.py -q` | ✅ PASS | **16 passed** in 3.86s,exit 0 |
| G5 | `python -m pytest tests/orchestrator/test_deploy_local.py -q` | ✅ PASS | **15 passed** in 3.32s,exit 0 |
| G6 | `python -m pytest tests/orchestrator/test_artifacts_manifest.py -q && python -m orchestrator.daemon.doccheck docs/deploy-runbook.md --require-section sudo --require-section venv --require-section nginx --require-section .env` | ✅ PASS | pytest **9 passed** in 0.45s,exit 0;doccheck `OK: docs/deploy-runbook.md`,exit 0 |
| G7 | `python -m orchestrator.daemon.doccheck document/business/T-2026-0901-004-部署契约/05_部署交付/演示记录.md --require-section 本地演示 --require-section 失败路径演示` | ✅ PASS | doccheck `OK`,exit 0 |
| G8 | `python -m orchestrator.daemon.doccheck document/business/T-2026-0901-004-部署契约/05_部署交付/演示记录.md --require-section 远程演示 --require-section 冒烟结果` | ✅ PASS | doccheck `OK`,exit 0 |

**8/8 验收命令全部通过。**

### 整体闸(design Checkpoint 5 / PRD 验收标准)

| 闸 | 命令 | 判定 | 证据 |
|---|---|---|---|
| 全量 pytest | `python -m pytest tests/ -q` | ⚠️ 环境性 FAIL(非交付缺陷) | **353 passed / 5 failed / 2 deselected** in 485s;5 个失败全部为 runner 对 `acceptance_cmd` 的 `bash -c` 独立复检路径(test_routing.py×4、test_e2e_circuitbreaker.py×1),本沙箱 bash 无法启动(见「环境」),失败签名 `assert 'pending' == 'done'` / `'retry:'`,与 G7 演示记录 L130 完全一致,均不在本单 scope(deploy 相关用例全绿) |
| pyflakes | `python -m pyflakes orchestrator/daemon/deploy.py orchestrator/daemon/artifacts.py tests/orchestrator/test_deploy_*.py tests/orchestrator/test_artifacts_manifest.py` | ✅ PASS | exit 0,零告警 |
| 池加载闸 | `python scripts/check-pool-load.py` | ✅ PASS | `checked 23 tickets, 0 bad`,exit 0 |

**5 个失败明细(全部环境性)**:`test_routing.py::test_p3_dispatches_ready_tasks`、
`test_routing.py::test_p3_lazy_loads_tasks_yaml`、`test_routing.py::test_acceptance_recheck_pass`、
`test_routing.py::test_retry_then_success`、`test_e2e_circuitbreaker.py::test_circuit_breaker_full_lifecycle`。
失败签名一致:`advance_once` 后任务 status 停在 `pending`——`acceptance_cmd`(如 `exit 0`)经
`bash -c` 复检时 bash 在本沙箱崩溃(rc=256),复检非零 → 任务不判 done。**用例逻辑本身与
T-2026-0901-004 交付无关**(deploy 模块 90/90 单测全绿);需在宿主/CI 环境复跑确认全绿。

### 演示记录证据交叉核验(G7/G8,doccheck 之外的内容层)

- **本地形态**(演示记录 L5–57):`python -m orchestrator.daemon.deploy T-2026-0901-004` 退出 0;
  dashboard 重启(首启 pid 46748 → 二次 45736)+ 本地冒烟 `http://127.0.0.1:8765/` → 200 PASS;
  发布记录三章(部署清单/冒烟结果/回滚方案)落盘后清理,未污染正式发布流。
- **失败路径**(演示记录 L59–124):临时登记 404 URL → 同 CLI 退出码 **1**、冒烟 FAIL 留痕;
  incident `T-2026-0901-006`(related_ticket=原单)自动建成 + 原单事件流 `incident_created` 回链;
  回滚说明(本地:`git checkout <上一 tag>` + 重启清单)可照抄;还原后重跑退出 0,证明失败确由登记 URL 触发。
- **远程形态**(演示记录 L134–189):真实 vite 构建(28.7s)→ 19.8MB 打包 → scp 上传 →
  解包 `releases/v0.5.0-14-g9b5f1c9` → 依赖/.env 差异检查 → current 原子翻转 →
  `sudo -n systemctl restart sk-api`(凭证形态 a:sudoers 单条 NOPASSWD + PermitRootLogin no 落地);
  冒烟全绿(`/api/health` 200、`/` 200 且资源指纹 = 新构建、`/api/providers` 200),进程 cwd 实测指向 release 代码。
- **.env 差异**:仅 key 名对比(服务器多 `SK2_AD_LLM_PROVIDER` 等、example 多 OLLAMA/ALI/VOLC 系列),
  值不回传/不入记录(R9 密钥不落库)✓。
- 演示记录如实登记三处环境变通(沙箱 EPERM→vite preload、GBK→PYTHONUTF8、project_dir 精简暂存区),
  均代码零改动;遗留建议(非本单阻塞):`.gitignore` 补登 `.orc-local/`、契约补「project_dir 交付物清单」约定。

## 结论

**通过(8/8 验收命令全绿 + 整体闸 check-pool-load 0 bad + pyflakes 净)。**

- 交付面:deploy 配置双形态登记、smoke 检查器、发布记录三章、remote/local 双流水线、
  P5 门禁扩章、演示记录双形态 + 失败路径,全部按契约生效;G7/G8 演示为真实执行留痕,非示例文本。
- 全量 pytest 353 passed / 5 failed 的 5 个失败均为**沙箱环境性**(`acceptance_cmd` 经 `bash -c`
  复检路径,bash 在本沙箱无法启动),与本单交付无关,已在宿主/CI 环境复跑即可判绿;无产品缺陷证据。
- 缺陷处置结论:无交付缺陷;唯一建议项(演示记录 L131/151 已记)为后续补登 `.gitignore` 与交付物清单约定,
  不阻塞本单放行。
