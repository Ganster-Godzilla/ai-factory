# 歧义澄清记录 — T-2026-0829-006

策略层五条开放问题已由 T-2026-0828-004《歧义澄清决议》逐条定案(档数/hotfix、工作量标尺、PM 降级权边界、频度口径、报备落点),本单不再重开。

本单实施层澄清:

| # | 问题 | 结论 | 依据 |
|---|---|---|---|
| Q1 | L2 档在本单的行为? | 字段值允许存在,行为完全同 L1(门禁/熔断/流程不裁剪) | 提案"只落 L1+L3 两档";L2 裁剪留后续单 |
| Q2 | L3 的 P4(p4_verifying→p5_ready)verdict 检查怎么处理? | L3 豁免验收报告产物,verdict 检查随产物一并豁免;"验收命令执行"由 P3 边 task verify 留痕承载(不可省四项之二) | 策略:QA 直跑 acceptance_cmd,verdict 记事件,免独立验收报告 |
| Q3 | 存量单(created_at 为空,如本单 006 自身)受影响吗? | 不受影响:load 兜底 L1 且 gate_required 按 D3 本就不追溯;本单自身走全礼仪仅因纪律,非门禁强制 | gates.py D3 存量豁免 |
| Q4 | PM 调整 level(set-level 命令)做不做? | 不做。本单只有建单指定一条路,无变更路径即无需 level_changed 事件;PM 调整权随 L2/L3-manual 后续单一并落 | 提案范围项只列"字段+cli new --level" |
| Q5 | approval 解析改动会不会影响 dashboard 审批中心既有行为? | 不会:L1/L2 单 approval_target 返回与现 APPROVALS 字典完全一致,仅 L3 在 p0_proposed 分叉 | 策略 L3 最短路形态 |

无未决歧义。
