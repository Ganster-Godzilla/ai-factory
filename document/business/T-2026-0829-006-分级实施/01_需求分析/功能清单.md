# 功能清单 — T-2026-0829-006

| # | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F1 | Ticket.level 字段 + validate 取值域 + 存量 load 兜底 L1 | P0 | ticket.py;分流判据的载体 |
| F2 | `orc new --level`(缺省 L1) | P0 | cli.py;建单必填口径(缺省保守) |
| F3 | 状态机 L3 条件边 ×2(p0_proposed→p3_queued / p5_releasing→done+monitoring_skipped) | P0 | statemachine.py;快速通道骨架 |
| F4 | `approval_target(ticket)` 级别感知审批解析,CLI+Dashboard 双入口共用 | P0 | statemachine.py + cli.py + app.py |
| F5 | 熔断参数按级别(L3 ≤2 重试、免会诊、circuit_exhausted 挂起) | P0 | circuitbreaker.py + runner.py |
| F6 | 门禁按级别裁剪(L3 只守不可省四项,P3 边 verify 保留) | P0 | gates.py(裁剪逻辑);artifacts.py 注释对齐 |
| F7 | Dashboard 列表 level 徽章列 + 详情页级别卡 | P1 | views.py + tickets.html + ticket.html |
| F8 | 全套 TDD 测试(R1-R6 逐项) | P0 | tests/orchestrator/ |

> P0=本单必交付;P1=同单交付但可独立降级(若 P1 受阻不卡 P0 合入,另起小单补)。
> L2 裁剪/frequency_alert/L3-manual/window_hours 不在本单(策略留后续单)。
