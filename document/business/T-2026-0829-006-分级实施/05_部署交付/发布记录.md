# T-2026-0829-006 发布记录:任务分级快速通道(L1 现状 + L3 落地)

## 合并清单

| 文件 | 变更 |
|---|---|
| orchestrator/daemon/ticket.py | +`VALID_LEVELS`/`level` 字段(兜底 L1)/validate 取值域/new_ticket level 参数 |
| orchestrator/daemon/cli.py | `orc new --level`;approve 改调 approval_target |
| orchestrator/daemon/statemachine.py | L3 条件边 ×2 + LEVEL_GATED_EDGES 守卫 + `approval_target()` + monitoring_skipped 事件 |
| orchestrator/dashboard/app.py | /approve 改调 approval_target(双入口同口径) |
| orchestrator/daemon/circuitbreaker.py | `next_action(task, level)`;L3 ≤2 免会诊 |
| orchestrator/daemon/runner.py | _fail_ladder 传 ticket.level |
| orchestrator/daemon/gates.py | check_gate L3 裁剪分支;抽三助手(cc 过闸,R15) |
| orchestrator/dashboard/templates/{base,tickets,ticket}.html | level 徽章列 + 详情级别卡 |
| tests/orchestrator/{test_level.py(新),test_gates.py,test_dashboard.py} | +20 用例 |
| document/business/T-2026-0829-006-分级实施/ | 提案/P1/P2/验收/发布全套 |
| orchestrator.yaml | datahub-demo 项目登记(T-2026-0907-003 现场补遗,与本单无关) |

提交:feature 分支 `4ab7066`(S1-S5)/`9078ed7`(P1P2)/`d1b1cd5`(P4)/`1316413`(补遗);
merge commit **`4488c4d`**(main);guard pre-merge 两拦均已处置(未推送=本分支自产提交,合并即携带;脏树=登记补遗单独成commit)。

## 版本

无独立版本号;main @ 4488c4d 即发布单元。

## 部署清单

本项目(ai-factory 编排器)未登记 deploy 脚本,显式声明:部署 = main 代码生效
+ Dashboard 进程重启加载新模板(level 徽章列/详情卡随模板生效)。
pool 数据零迁移:存量 76 单无 level 字段,load 兜底 L1,行为不变。

## 冒烟结果

- `pytest tests/`(重构后第二轮,feature 树=main 树):**451 passed, 2 deselected**(769s)
- pyflakes 改动文件零告警;check-pool-load 76 单 0 bad;check-complexity PASS
- L3 演示路径(隔离 pool):建单→approve 直落 p3_queued(零产物);L1 对照被产物门禁拦;
  p5_releasing→done 事件 monitoring_skipped=true;L1 误走快速边 IllegalTransition
- Dashboard 重启后冒烟:`/tickets` 200,级别列渲染(见下"部署执行"行)

## 回滚方案

纯增量(字段缺省 L1 / 新边仅 L3 可走 / L1 全路径行为零变化),无数据迁移:

```
git revert 4488c4d && git push origin main
# 重启 Dashboard 加载旧模板
```

回退后 L3 快速边消失(全部回 L1 全流程),存量单无任何影响。
