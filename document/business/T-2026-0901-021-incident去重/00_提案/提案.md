# 提案:create_incident 去重(防票据雪崩)

## 问题

发布/冒烟失败自动建 incident 单无去重:重试循环每次失败都新建一张。2026-09-01 007 实证:1.5h 雪崩 9 张重复单(010-018),清理后又生 020——循环仍在跑。

## 方向

建单前查 pool:原单已有未关闭(非 done/closed)incident → 复用,append_event 追加证据;否则才新建。deploy.py 与 runner.py 两条建单路径统一收口到一个模块。

## 范围

- 新建 orchestrator/daemon/incident.py(create_incident 含去重);deploy.py/runner.py 改调用
- 单测:首建/复用/关闭后再建三态
- 重复单现场已清(7+1 张关闭,010 保留)

## 不做

- 不改 incident 单的生命周期(仍人工推进);不做自动关单/自动合并
