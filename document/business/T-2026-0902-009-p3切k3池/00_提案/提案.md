# 提案:P3 执行层切 k3 订阅池(基座)

## 问题

DeepSeek 余额耗尽(¥22→0),按量付费现金流断裂;k3 四账号全是订阅(沉没成本,边际 0)。昨天 91M tokens/¥16 的 runner 消耗全部可转订阅池。这是后续所有任务的基座。

## 方向

runner dev/qa/release/sre 从 dsh(DeepSeek)切到 k3 适配器(claude -p 经 relay);relay failover 组扩成多层逃生梯:kimi1-4(订阅)→ glm-flash(免费)→ ds-flash(廉价溢出,DS 充值后自动生效)→ zen-k3(昂贵,$30 余额硬帽)。每层 429/冷却自动滑下一层。

## 范围

- relay:kimi4(k3-d)入池;ds-flash/glm-flash 翻译后端(复用 relay-translate.js);测试与顺位演练
- ai-factory:runner 角色路由切换 + 真实小切片首验;水位闸 150M→600M(warn-only 观察);Dashboard DS 现金字段标注
- 生产:重启 relay + 全链路验收

## 不做

- 不做 per-ticket k3 归因(relay 架构上无按单通道,沿用池级水位)
- 不改 zen-k3 语义(仍为最后保险);不动 GLM/DS 官方控制台配置
- 不删 dsh 适配器(保留作对照/回退路径)
