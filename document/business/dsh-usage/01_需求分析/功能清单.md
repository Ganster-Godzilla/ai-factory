# 功能清单 —— T-2026-0829-002 dsh usage 会话文件解析

| # | 功能 | 优先级 | 验收标准 |
|---|---|---|---|
| F1 | 会话文件定位:转义快路径 + 读 session 头 cwd 匹配兜底 | P0 | 给定 workdir 命中正确 session 文件;转义规则大小写/分隔符变体不误判 |
| F2 | 多帧 zstd 流式解压 | P0 | 单帧文件与多帧追加文件都读全(实证:decompress() 只读首帧的坑) |
| F3 | usage 记录过滤汇总:time>=run_start、(turn,step) 去重取末 | P0 | 汇总三值与手工核算一致;旧会话不混入 |
| F4 | 费率表配置:orchestrator.yaml rates 段(deepseek/glm) | P0 | cost 精确到 4 位小数;glm=0 且 tokens 照记;改配置生效不改码 |
| F5 | 优先级链:trailer > 会话 > 双缺硬判负 | P0 | 三态各有用例钉死;双缺 failed+usage_missing+est 照估入账(estimated=True) |
| F6 | 解析容错 | P0 | 文件损坏/缺 zstandard/目录不存在 → 落双缺,不抛异常 |
| F7 | tokens 口径:cacheRead 不进 tokens 字段只进 cost | P0 | result.tokens=={input,output}(新鲜量),cost 含 cacheRead 贡献 |
| F8 | 依赖登记 | P1 | zstandard 入 requirements;装包镜像注释(运维教训) |
