# 发布记录:T-2026-0902-016

## 合并清单

- `4eb44c3` feat(S1):statemachine.resume 加 force 参数+同因检测
- `1e7fc1d` feat(S2):cli resume --force 透传
- `7466c2b` feat(S3):dashboard /resume 同因强制勾选

全部经 TDD 先红后绿,切片即合 main;P4 验收报告通过,过 verdict 闸放行。

## 版本

- 部署版本:`v1.3.0-16-gf1f84a6`(git describe,工作区干净)
- 变更面:`orchestrator/daemon/statemachine.py`(resume force+同因检测)、
  `orchestrator/daemon/cli.py`(--force)、`orchestrator/dashboard/app.py`+模板
  (同因强制勾选)、对应测试
- 影响:自本版起,resume 校验"本次与上次挂起是否同一根因";同因须显式确认
  (--force / dashboard 勾选)才恢复,根因已变则正常放行(R12)

## 部署清单

- 版本:`v1.3.0-16-gf1f84a6`
- 目标:`local`
- 时间:`2026-09-02T16:09:44.905088+00:00`
- 重启进程:dashboard(旧 pid 9408 → 新 pid 14896)

## 冒烟结果

- `http://127.0.0.1:8765/` → 200, 2496ms → PASS

**总体判定:全绿,发布成功**

## 回滚方案

1. git tag 回切(命令可照抄):`git checkout <上一 tag>`
2. 重启长驻进程(逐条可照抄):
   - dashboard:`python -m orchestrator.daemon.cli dashboard --port 8765`
3. 按部署冒烟清单复验,全 200 才算回滚成功

