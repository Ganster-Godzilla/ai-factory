# 歧义澄清记录:resume 同因幂等提示

- 工单:`pool/tickets/T-2026-0902-016.yaml`

## 已澄清

| 问题 | 结论 | 依据 |
|---|---|---|
| "同因"如何判定? | 比对事件流最近一次 `suspended` 的 `reason_code` 与当前挂起 reason_code,相等即同因 | reason_code 是挂起时已结构化的字段,无需读代码诊断 |
| 同因时默认放行还是拦截? | 默认**警告+不执行**,显式确认(CLI `--force`/dashboard 勾选)才 resume | 确定性 bug 重试无果,默认放行等于没修;但保留人工强制口子 |
| 是否自动判断"根因是否已修"? | 不做;仅以 reason_code 是否变化作代理 | 自动诊断超范围,reason_code 变化是够用的信号 |
| 改动入口范围? | CLI `orc resume` + dashboard `/resume` 两处,语义一致 | resume 仅这两处(statemachine.resume) |

## 待澄清(设计期定)

- "连续同因"的窗口:只比对**最近一次**挂起,还是看最近 N 次?设计期定,倾向最近一次
  (简单且覆盖本次事故形态)。

## 无其余歧义

除上述外,需求边界(只在 resume 入口加幂等提示,不改 suspend 触发逻辑)无歧义。
