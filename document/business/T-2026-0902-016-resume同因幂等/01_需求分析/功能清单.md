# 功能清单:resume 同因幂等提示

- 工单:`pool/tickets/T-2026-0902-016.yaml`

## 功能清单

| 功能 | 描述 | 优先级 |
|---|---|---|
| F1 同因检测 | 从事件流取最近一次 `suspended` 的 reason_code,与当前挂起 reason_code 比对,判定是否同因 | P0 |
| F2 resume 警告+确认 | 同因 → 警告"根因未变请先修",CLI `--force`/dashboard 确认才执行;否则不 resume | P0 |
| F3 正常放行路径 | reason_code 不同或无历史挂起 → 无警告直接恢复 | P0 |
| F4 双入口一致 | CLI `orc resume` 与 dashboard `/resume` 同因检测与确认语义一致 | P1 |
| F5 留痕 | 同因警告/强制恢复写事件流,可审计 | P1 |
| F6 回归保护 | 现有 resume 正常路径行为不变,全套测试绿 | P1 |

优先级口径:P0=本单核心、不做则连撞复发;P1=一致性/可观测,缺了不阻塞主链路但须补齐。
