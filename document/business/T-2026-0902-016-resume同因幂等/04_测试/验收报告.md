# 验收报告:resume 同因幂等提示(防确定性 bug 连撞)

- 工单:`pool/tickets/T-2026-0902-016.yaml`
- 依据:`../01_需求分析/prd.md` 验收标准 + `../02_设计文档/design.md` Checkpoints
- 执行角色:qa(黑盒,只看验收命令与外部行为)

## 环境

| 项 | 值 |
|---|---|
| 机器 | Windows 11 Home China (10.0.26200) |
| Python | 3.11.5 |
| 代码 | main @ `7466c2b`(S3 提交) |
| 日期 | 2026-09-02 |

## 范围

- S1/S2/S3 三条切片验收命令逐条执行
- PRD 验收标准 AC1-AC6 逐条对照

## 用例结果

| # | 用例 | 命令 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | S1 statemachine.resume force+同因检测 | `pytest tests/orchestrator/test_statemachine.py -q` | **通过** | `16 passed`(5 新 resume 用例) |
| 2 | S2 cli resume --force 透传 | `pytest tests/orchestrator/test_cli.py -q` | **通过** | `7 passed`(同因拦截 rc=1/同因 force rc=0) |
| 3 | S3 dashboard 同因强制勾选 | `pytest tests/orchestrator/test_dashboard.py -q` | **通过** | `47 passed`(同因拦截非 302/同因 force 302) |
| 4 | AC1 同因连续 resume(第二次)未确认 → 警告+不执行 | S1/S2/S3 拦截用例 | **通过** | statemachine/cli/dashboard 三层均拦,状态不变 |
| 5 | AC2 同因+force/勾选 → 正常恢复+留痕 | S1 `force=True`(forced=True)/S2 `--force`/S3 勾选 | **通过** | resumed 事件 forced=True;两入口均恢复 |
| 6 | AC3 reason_code 不同 → 直接放行 | S1 `test_resume_different_reason_passes` | **通过** | 无警告恢复 |
| 7 | AC4 首次 resume(无历史挂起)→ 放行 | S1 `test_resume_first_time_no_history_passes` | **通过** | 正常恢复 |
| 8 | AC5 CLI 与 dashboard 语义一致 | S2/S3 对照 | **通过** | 两入口同因检测+确认语义一致 |
| 9 | AC6 现有 resume 零回归 | `pytest tests/ -q` | **通过** | 全套绿(见下) |

## 边界补充

- 存量旧挂起 reason_code=None → 不参与同因判定,放行(S1 `test_resume_none_reason_not_treated_same`),不误伤存量。

## 全套回归

`pytest tests/ -q`(015+016 合并后终跑):**421 passed, 2 deselected, 360s, exit 0**;
`python scripts/check-pool-load.py`:**checked 54 tickets, 0 bad**。

## 结论

**通过。** 三条切片验收命令全绿,PRD AC1-AC6 逐条满足。R12 落地:resume 现在校验"本次与上次是否同一根因",确定性 bug 不再能被无脑 resume 连撞;同因须显式确认(--force/勾选)才恢复,根因已变则正常放行。
