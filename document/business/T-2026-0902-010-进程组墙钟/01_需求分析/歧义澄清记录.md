# 歧义澄清记录

- "看门狗随 advance 进程生命周期"与"父死不灭"是否矛盾:不矛盾——需求层只要求父进程死亡后墙钟仍生效且不做常驻守护进程/系统服务;具体载体(detached 子进程、OS 级 Job 杀组等)属实现选型,留 P2 架构师定。
- 墙钟取值:沿用 packet.timeout 既有分级(task.timeout/ROLE_TIMEOUT),看门狗不加新数值、不改既有数值;父活着时看门狗不抢跑。
- 终止方式:默认先礼后兵(终止信号→宽限→强杀)为 P1 增强;P0 只保证按组终止成立,单级强杀也可过验收。
- 与 T-2026-0902-011 的边界:011 治 .env 假状态(sk-video-studio 项目),本单只治孤儿进程无人限时;两单根因不同,不合并。
- 其余:无歧义。
