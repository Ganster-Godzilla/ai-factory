# 验收报告:T-2026-0902-010 runner 进程组墙钟看门狗(适配器无关)

- 工单:`pool/tickets/T-2026-0902-010.yaml`(验收时状态 p4_verifying)
- 依据:`../01_需求分析/prd.md` 验收标准 + `../02_设计文档/design.md` Checkpoints 收口项
- 执行角色:qa(黑盒,只看验收命令与外部行为,不看实现)

## 环境

| 项 | 值 |
|---|---|
| 机器 | Windows 11 Home China (10.0.26200) |
| Python | 3.11.5 |
| 代码 | main @ `8cb30b7`(工作区干净) |
| 日期 | 2026-09-02 |

## 范围

- S1/S2/S3 三条切片验收命令逐条执行
- PRD 验收标准 4 条逐条对照
- 设计 Checkpoints 整单收口 3 项(pytest 全绿 / check-pool-load 0 bad / adapters 零改动)

## 用例结果

| # | 用例 | 命令 / 方法 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | S1 父活场景:guard 正常返回、注册表闭环、helper 退出、孙泄漏清扫、killed→timeout 改写、进程组归属 | `pytest tests/test_watchdog.py -x -q` | **通过** | `6 passed in 6.88s` |
| 2 | S2 孤儿实证:杀父+短墙钟+sleep 复现 009 游荡,断言进程组被终止、`watchdog_killed` 事件字段齐(pid/墙钟/工单/任务)、任务 timeout 判负 | `pytest tests/test_watchdog_orphan.py -x -q` | **通过** | `1 passed in 4.10s` |
| 3 | S3 runner 接线:全角色路径冒烟(五角色/dev/consult 经 `_run_with_watchdog`)+ 父活语义回归 | `pytest tests/test_runner_watchdog.py tests/test_runner.py -x -q` | **通过** | `8 passed in 17.24s` |
| 4 | PRD 验收 2:适配器文件零改动 | `git diff 0983b4a~1..HEAD -- orchestrator/adapters/`(0983b4a=本单 P2 提交) | **通过** | diff 为空(exit 0,无输出) |
| 5 | PRD 验收 4 / 收口:`pytest tests/` 全绿 | `pytest tests/ -q` | **失败** | 收集期报错:`import file mismatch`——`tests/test_runner.py` 与既有 `tests/orchestrator/test_runner.py` 同名冲突(无 `__init__.py`,pytest 按 basename 建模块名)。清 `__pycache__` 后复现,排除缓存因素 |
| 6 | 隔离 D1 后全套 | `pytest tests/ -q --ignore=tests/test_runner.py` | **失败** | `396 passed, 1 failed in 377.65s`;失败项 `tests/test_watchdog.py::test_grandchild_leak_swept_by_helper`,打印 `FAIL: helper 没有写 watchdog_killed 事件` |
| 7 | 用例 6 失败项隔离复跑 | `pytest tests/test_watchdog.py::test_grandchild_leak_swept_by_helper -x -q` | 通过 | `1 passed in 2.80s` —— 隔离过、全套挂,时序/负载敏感 |
| 8 | PRD 验收 4 / 收口:`check-pool-load` 0 bad | `python scripts/check-pool-load.py` | **失败** | `checked 51 tickets, 1 bad`;bad 为 `T-2026-0902-013.yaml`:R10 "done 状态 tasks 为空"——**非本单工单**,但收口项按字面不达标 |

## 缺陷清单

| 缺陷 | 严重度 | 归因 | 说明与处置建议 |
|---|---|---|---|
| D1:全套收集冲突 | P0(卡收口) | **本单引入**(S3 补档 `370f55c` 新建 `tests/test_runner.py`,与 G7 期既有 `tests/orchestrator/test_runner.py` 撞名) | 处置:重命名(如 `tests/test_runner_parent_alive.py`)或并入 `tests/test_runner_watchdog.py`;改后全套可收集 |
| D2:孙泄漏清扫用例全套上下文不稳 | P1 | **本单引入**(测试时序敏感:全套负载下 helper 未在测试等待窗口内写 `watchdog_killed`,隔离复跑即过) | 处置:等待改轮询/放宽窗口;该用例测的正是本单核心机制,稳定性须达标,否则验收结果不可复现 |
| D3:check-pool-load 1 bad(T-2026-0902-013 done 且 tasks 为空) | P1 | **非本单**(013 的 R10 违反) | 处置:013 侧补 tasks 或回退状态;与本单代码无关,但"0 bad"收口项须等其清账 |

通过项小结:三条切片验收命令(用例 1-3)各自单跑全绿,孤儿实证三条断言(按组终止/事件字段齐/timeout 判负)均满足;适配器零改动有 diff 实证;全套中除 D1/D2 外 396 项全绿,父活语义回归无破坏。

## 结论

**不通过,P4 挂起,退回处置。**

- 阻塞项 D1(P0)、D2(P1)均为本单引入,须由实现侧修复后复验;D3 为池内他单欠账,需 013 清账或经审批人明示豁免口径。
- 复验范围:D1/D2 修复后重跑 `pytest tests/ -q`(全绿)+ `python scripts/check-pool-load.py`(0 bad 或审批人签认的豁免);用例 1-4 已过项无需重跑,但 D2 修复触及 `test_watchdog.py` 时用例 1 须一并重跑。

## 复验(2026-09-02,实现侧修复后)

| 缺陷 | 修复 | 复验命令 | 结果 |
|---|---|---|---|
| D1 收集冲突 | `git mv tests/test_runner.py → tests/test_runner_parent_alive.py`(scope 内,S3 补档);既存 `tests/orchestrator/test_runner.py`(G7,536 行)零改动;`test_runner_watchdog.py` 两处注释引用同步 | `pytest tests/ -q --collect-only` | **通过**:399/401 collected,0 error(原 397+1 collection error) |
| D2 孙泄漏用例不稳 | `test_watchdog.py` `_killed_events` 等待窗 20s→45s(全套负载下 helper 到点写事件超窗;不碰核心机制) | `pytest tests/test_watchdog.py -q` + D2 单跑×2 | **通过**:6 passed;D2 连跑 3.31s/3.24s 稳定 |
| D3 013 R10 欠账 | 013 补登 S1 留痕(提交 edd39e6,快轨执行补漏)+ `tasks_backfilled` 事件 | `python scripts/check-pool-load.py` | **通过**:checked 52 tickets, 0 bad |
| 收口:全套 | 上述三项合并 | `pytest tests/ -q` | **通过**:399 passed, 2 deselected, 356.13s, exit 0 |

复验结论:**D1/D2/D3 全部清除,收口项(pytest 全绿 + check-pool-load 0 bad)达标,可进入发布。**
